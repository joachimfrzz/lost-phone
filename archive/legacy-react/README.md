# Legacy React / Vite Creator

Archivé juillet 2026 lors de la restructuration **LostPhone Studio**.

- **Raison :** pivot SwiftUI natif ([`../../PIVOT-SWIFTUI.md`](../../PIVOT-SWIFTUI.md))
- **Contenu :** `src/`, `vite.config.ts`, `index.html`, `tsconfig*.json`, `capacitor.config.ts`

Pour restaurer temporairement (déconseillé) :

```bash
# Copier depuis archive vers racine — mettre à jour package.json depuis git history
```

Scripts associés : [`../scripts-legacy/`](../scripts-legacy/)
