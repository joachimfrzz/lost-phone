import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredSnapchat`).
struct LpspVendoredSnapchatRootView: View {
    var body: some View {
        VendoredSnapchatContentView()
    }
}
