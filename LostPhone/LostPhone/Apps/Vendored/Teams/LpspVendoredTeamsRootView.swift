import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredTeams`).
struct LpspVendoredTeamsRootView: View {
    var body: some View {
        VendoredTeamsContentView()
    }
}
