import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredChatGPT`).
struct LpspVendoredChatGPTRootView: View {
    var body: some View {
        VendoredChatGPTContentView()
    }
}
