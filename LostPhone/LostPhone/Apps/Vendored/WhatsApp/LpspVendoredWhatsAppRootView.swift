import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredWhatsApp`).
struct LpspVendoredWhatsAppRootView: View {
    var body: some View {
        VendoredWhatsAppContentView()
    }
}
