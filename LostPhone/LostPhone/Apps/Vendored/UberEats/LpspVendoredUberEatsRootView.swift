import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredUberEats`).
struct LpspVendoredUberEatsRootView: View {
    var body: some View {
        VendoredUberEatsContentView()
    }
}
