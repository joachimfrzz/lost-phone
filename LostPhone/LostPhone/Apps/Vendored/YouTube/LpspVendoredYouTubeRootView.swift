import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredYouTube`).
struct LpspVendoredYouTubeRootView: View {
    var body: some View {
        VendoredYouTubeContentView()
    }
}
