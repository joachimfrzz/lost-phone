import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredPhantom`).
struct LpspVendoredPhantomRootView: View {
    var body: some View {
        VendoredPhantomContentView()
    }
}
