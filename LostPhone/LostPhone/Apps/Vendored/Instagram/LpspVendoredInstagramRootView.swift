import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredInstagram`).
struct LpspVendoredInstagramRootView: View {
    var body: some View {
        VendoredInstagramContentView()
    }
}
