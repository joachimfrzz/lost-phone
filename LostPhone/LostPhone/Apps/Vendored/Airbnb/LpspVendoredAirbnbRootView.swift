import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredAirbnb`).
struct LpspVendoredAirbnbRootView: View {
    var body: some View {
        VendoredAirbnbContentView()
    }
}
