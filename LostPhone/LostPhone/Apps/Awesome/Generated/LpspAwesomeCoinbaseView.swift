import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/finance/coinbase/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeCoinbaseView: View {
    var body: some View {
        TabView {
            CoinbaseTabHost(title: "Accueil", icon: "house.fill") { CoinbaseScreenFactory.screen(for: "Accueil") }
                .tabItem { Label("Accueil", systemImage: "house.fill") }
            CoinbaseTabHost(title: "Cartes", icon: "creditcard.fill") { CoinbaseScreenFactory.screen(for: "Cartes") }
                .tabItem { Label("Cartes", systemImage: "creditcard.fill") }
            CoinbaseTabHost(title: "Plus", icon: "ellipsis") { CoinbaseScreenFactory.screen(for: "Plus") }
                .tabItem { Label("Plus", systemImage: "ellipsis") }
        }
        .tint(CoinbaseTokens.cbBlue)
    }
}

private struct CoinbaseTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum CoinbaseScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("activity"):
            CoinbaseHomeScreen()
        case let t where t.contains("card") || t.contains("invest") || t.contains("crypto") || t.contains("market") || t.contains("trade") || t.contains("financ"):
            CoinbaseCardsScreen()
        case let t where t.contains("recipient") || t.contains("plus"):
            CoinbaseMoreScreen()
        default:
            CoinbaseGenericListScreen(title: tab)
        }
    }
}
private enum CoinbaseTokens {
    static let cbBlue = Color(red: 0.00, green: 0.322, blue: 1.00)  // #0052FF
    static let cbBluePressed = Color(red: 0.00, green: 0.251, blue: 0.800) // #0040CC
    static let cbBlueTint = Color(red: 0.898, green: 0.929, blue: 1.00) // #E5EDFF
    static let cbBlueDark = Color(red: 0.231, green: 0.424, blue: 1.00) // #3B6CFF (dark-mode)
    static let cbBlack = Color(red: 0.039, green: 0.043, blue: 0.051) // #0A0B0D
    static let cbCharcoal = Color(red: 0.102, green: 0.110, blue: 0.122) // #1A1C1F
    static let cbBitcoin = Color(red: 0.969, green: 0.576, blue: 0.102) // #F7931A
    static let cbEthereum = Color(red: 0.384, green: 0.494, blue: 0.918) // #627EEA
    static let cbUSDC = Color(red: 0.153, green: 0.459, blue: 0.792) // #2775CA
    static let cbSolana = Color(red: 0.600, green: 0.271, blue: 1.00)  // #9945FF
    static let cbCardano = Color(red: 0.00, green: 0.200, blue: 0.678)  // #0033AD
    static let cbTether = Color(red: 0.149, green: 0.631, blue: 0.482) // #26A17B
    static let cbCanvas = Color(red: 1.00, green: 1.00, blue: 1.00)    // #FFFFFF
    static let cbSurfaceGray = Color(red: 0.969, green: 0.973, blue: 0.980) // #F7F8FA
    static let cbSurfaceGray2 = Color(red: 0.933, green: 0.941, blue: 0.953) // #EEF0F3
    static let cbDivider = Color(red: 0.882, green: 0.894, blue: 0.910) // #E1E4E8
    static let cbTextPrimary = Color.cbBlack
    static let cbTextSecondary = Color(red: 0.357, green: 0.380, blue: 0.431) // #5B616E
    static let cbTextTertiary = Color(red: 0.502, green: 0.525, blue: 0.561) // #80868F
    static let cbTextMuted = Color(red: 0.627, green: 0.643, blue: 0.667) // #A0A4AA
    static let cbSuccess = Color(red: 0.020, green: 0.694, blue: 0.412) // #05B169
    static let cbSuccessTint = Color(red: 0.902, green: 0.969, blue: 0.937) // #E6F7EF
    static let cbLoss = Color(red: 0.812, green: 0.125, blue: 0.184) // #CF202F
    static let cbLossTint = Color(red: 0.988, green: 0.906, blue: 0.914) // #FCE7E9
    static let cbWarning = Color(red: 0.961, green: 0.651, blue: 0.137) // #F5A623
    static let cbDarkCanvas = Color.cbBlack
    static let cbDarkSurface1 = Color(red: 0.075, green: 0.082, blue: 0.102) // #13151A
    static let cbDarkSurface2 = Color(red: 0.118, green: 0.125, blue: 0.149) // #1E2026
    static let cbDarkDivider = Color(red: 0.165, green: 0.180, blue: 0.212) // #2A2E36
    static let cbDarkTextPri = Color.white
    static let cbDarkTextSec = Color(red: 0.627, green: 0.643, blue: 0.667) // #A0A4AA
}

private enum CoinbaseFonts {
    static let cbPortfolioHero = Font.system(size: 40, weight: .regular)
    static let cbBuyAmount = Font.system(size: 56, weight: .regular)
    static let cbScreenTitle = Font.system(size: 28, weight: .regular)
    static let cbSectionHeader = Font.system(size: 20, weight: .regular)
    static let cbAssetTitle = Font.system(size: 16, weight: .regular)
    static let cbAssetPrice = Font.system(size: 16, weight: .regular)
    static let cbAssetChange = Font.system(size: 13, weight: .regular)
    static let cbBody = Font.system(size: 15, weight: .regular)
    static let cbBodySmall = Font.system(size: 13, weight: .regular)
    static let cbTicker = Font.system(size: 13, weight: .regular)
    static let cbTab = Font.system(size: 10, weight: .regular)
    static let cbAllCaps = Font.system(size: 11, weight: .regular)
    static let cbRangeChip = Font.system(size: 13, weight: .regular)
    static let cbButton = Font.system(size: 16, weight: .regular)
    static let cbButtonSmall = Font.system(size: 13, weight: .regular)
    static let cbWalletAddr = Font.system(size: 13, weight: .regular)
}
private struct CoinbaseHomeScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 20) {
                    VStack(alignment: .leading, spacing: 8) {
                        Text("Solde total").font(.system(size: 14)).foregroundStyle(CoinbaseTokens.cbTextSecondary)
                        Text("2 847,50 €").font(.system(size: 36, weight: .bold)).foregroundStyle(CoinbaseTokens.cbTextPrimary)
                        HStack(spacing: 16) {
                            Label("Ajouter", systemImage: "plus")
                            Label("Envoyer", systemImage: "arrow.up.right")
                            Label("Échanger", systemImage: "arrow.left.arrow.right")
                        }.font(.system(size: 14, weight: .semibold)).foregroundStyle(CoinbaseTokens.cbBlue).padding(.top, 4)
                    }.frame(maxWidth: .infinity, alignment: .leading)
                    RoundedRectangle(cornerRadius: 16).fill(LinearGradient(colors: [CoinbaseTokens.cbBlue, CoinbaseTokens.cbBlue.opacity(0.6)], startPoint: .topLeading, endPoint: .bottomTrailing))
                        .frame(height: 180).overlay(alignment: .bottomLeading) {
                            VStack(alignment: .leading) { Text("Carte Virtuelle").font(.system(size: 13)).foregroundStyle(.white.opacity(0.8)); Text("•••• 4829").font(.system(size: 22, weight: .bold)).foregroundStyle(.white) }.padding(20)
                        }
                    VStack(alignment: .leading, spacing: 12) {
                        Text("Transactions").font(.system(size: 18, weight: .bold))
                        ForEach(CoinbaseDemoData.transactions) { tx in CoinbaseTransactionRow(tx: tx) }
                    }
                }.padding()
            }.background(CoinbaseTokens.cbCanvas.ignoresSafeArea()).navigationTitle("Coinbase")
        }
    }
}

private struct CoinbaseTransactionRow: View {
    let tx: CoinbaseTransaction
    var body: some View {
        HStack(spacing: 12) {
            Circle().fill(tx.color.opacity(0.2)).frame(width: 44, height: 44).overlay(Image(systemName: tx.icon).foregroundStyle(tx.color))
            VStack(alignment: .leading) { Text(tx.title).font(.system(size: 16, weight: .medium)); Text(tx.date).font(.system(size: 13)).foregroundStyle(.secondary) }
            Spacer()
            Text(tx.amount).font(.system(size: 16, weight: .semibold)).foregroundStyle(tx.amount.hasPrefix("-") ? .primary : .green)
        }
    }
}

private struct CoinbaseCardsScreen: View {
    var body: some View { NavigationStack { Text("Gérez vos cartes").padding().navigationTitle("Cartes") } }
}

private struct CoinbaseMoreScreen: View {
    var body: some View {
        NavigationStack { List { Section("Compte") { Label("Profil", systemImage: "person"); Label("Sécurité", systemImage: "lock") } }.navigationTitle("Plus") }
    }
}

private struct CoinbaseTransaction: Identifiable { let id = UUID(); let title: String; let date: String; let amount: String; let icon: String; let color: Color }
private enum CoinbaseDemoData {
    static let transactions: [CoinbaseTransaction] = [
        .init(title: "Monoprix", date: "Aujourd'hui", amount: "-24,80 €", icon: "cart.fill", color: .orange),
        .init(title: "Virement reçu", date: "Hier", amount: "+150,00 €", icon: "arrow.down.left", color: .green),
        .init(title: "Netflix", date: "28 juin", amount: "-13,49 €", icon: "play.tv.fill", color: .red),
    ]
}

private struct CoinbaseGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(CoinbaseTokens.cbBlue.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}
