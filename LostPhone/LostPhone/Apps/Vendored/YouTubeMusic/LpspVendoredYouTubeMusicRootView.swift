import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredYouTubeMusic`).
struct LpspVendoredYouTubeMusicRootView: View {
    var body: some View {
        VendoredYouTubeMusicContentView()
    }
}
