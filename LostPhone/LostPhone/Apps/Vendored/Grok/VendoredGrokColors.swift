import SwiftUI

enum VendoredGrokTheme {
    static let background = Color(hex: "#FAF8F6")
    static let card = Color.white
    static let border = Color(hex: "#E4E1DC")
    static let textPrimary = Color(hex: "#1E1E1E")
    static let textSecondary = Color(hex: "#6E6D6D")
    static let redIndicator = Color(hex: "#FF4E42")
}
