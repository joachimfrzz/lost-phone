import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/finance/revolut/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeRevolutView: View {
    var body: some View {
        TabView {
            RevolutTabHost(title: "Home", icon: "house.fill") { RevolutScreenFactory.screen(for: "Home") }
                .tabItem { Label("Home", systemImage: "house.fill") }
            RevolutTabHost(title: "Invest", icon: "chart.line.uptrend.xyaxis") { RevolutScreenFactory.screen(for: "Invest") }
                .tabItem { Label("Invest", systemImage: "chart.line.uptrend.xyaxis") }
            RevolutTabHost(title: "Crypto", icon: "bitcoinsign.circle.fill") { RevolutScreenFactory.screen(for: "Crypto") }
                .tabItem { Label("Crypto", systemImage: "bitcoinsign.circle.fill") }
            RevolutTabHost(title: "Lifestyle", icon: "sparkles") { RevolutScreenFactory.screen(for: "Lifestyle") }
                .tabItem { Label("Lifestyle", systemImage: "sparkles") }
            RevolutTabHost(title: "Cards", icon: "creditcard.fill") { RevolutScreenFactory.screen(for: "Cards") }
                .tabItem { Label("Cards", systemImage: "creditcard.fill") }
        }
        .tint(RevolutTokens.revBrand)
    }
}

private struct RevolutTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum RevolutScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("activity"):
            RevolutHomeScreen()
        case let t where t.contains("card") || t.contains("invest") || t.contains("crypto") || t.contains("market") || t.contains("trade") || t.contains("financ"):
            RevolutCardsScreen()
        case let t where t.contains("recipient") || t.contains("plus"):
            RevolutMoreScreen()
        default:
            RevolutGenericListScreen(title: tab)
        }
    }
}
private enum RevolutTokens {
    static let revCanvas = Color(red: 0.039, green: 0.039, blue: 0.059) // #0A0A0F
    static let revSurface1 = Color(red: 0.086, green: 0.086, blue: 0.122) // #16161F
    static let revSurface2 = Color(red: 0.118, green: 0.118, blue: 0.165) // #1E1E2A
    static let revSurface3 = Color(red: 0.157, green: 0.157, blue: 0.227) // #28283A
    static let revDivider = Color(red: 0.165, green: 0.165, blue: 0.220) // #2A2A38
    static let revBorder = Color(red: 0.200, green: 0.200, blue: 0.290) // #33334A
    static let revTextPrimary = Color.white                                  // #FFFFFF
    static let revTextSecondary = Color(red: 0.604, green: 0.604, blue: 0.667) // #9A9AAA
    static let revTextTertiary = Color(red: 0.416, green: 0.416, blue: 0.494) // #6A6A7E
    static let revGradStart = Color(red: 0.357, green: 0.420, blue: 1.000) // #5B6BFF
    static let revGradEnd = Color(red: 0.612, green: 0.420, blue: 1.000) // #9C6BFF
    static let revBrand = Color(red: 0.420, green: 0.357, blue: 1.000) // #6B5BFF
    static let revBrandPressed = Color(red: 0.337, green: 0.282, blue: 0.839) // #5648D6
    static let revBrandTint = Color(red: 0.110, green: 0.106, blue: 0.200) // #1C1B33
    static let revIncome = Color(red: 0.122, green: 0.820, blue: 0.482) // #1FD17B
    static let revSpend = Color(red: 1.000, green: 0.353, blue: 0.416) // #FF5A6A
    static let revWarn = Color(red: 1.000, green: 0.698, blue: 0.247) // #FFB23F
    static let revCrypto = Color(red: 0.969, green: 0.788, blue: 0.282) // #F7C948
}

private enum RevolutFonts {
    static let revBalance = Font.system(size: 40, weight: .regular)
    static let revTitleLarge = Font.system(size: 28, weight: .regular)
    static let revSection = Font.system(size: 22, weight: .regular)
    static let revTileBalance = Font.system(size: 22, weight: .regular)
    static let revSubsection = Font.system(size: 18, weight: .regular)
    static let revAmount = Font.system(size: 16, weight: .regular)
    static let revMerchant = Font.system(size: 16, weight: .regular)
    static let revBody = Font.system(size: 15, weight: .regular)
    static let revButton = Font.system(size: 16, weight: .regular)
    static let revMeta = Font.system(size: 13, weight: .regular)
    static let revLabelUpper = Font.system(size: 11, weight: .regular)
    static let revTab = Font.system(size: 10, weight: .regular)
    static let revCaption = Font.system(size: 11, weight: .regular)
}
private struct RevolutHomeScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 20) {
                    VStack(alignment: .leading, spacing: 8) {
                        Text("Solde total").font(.system(size: 14)).foregroundStyle(RevolutTokens.revTextSecondary)
                        Text("2 847,50 €").font(.system(size: 36, weight: .bold)).foregroundStyle(RevolutTokens.revTextPrimary)
                        HStack(spacing: 16) {
                            Label("Ajouter", systemImage: "plus")
                            Label("Envoyer", systemImage: "arrow.up.right")
                            Label("Échanger", systemImage: "arrow.left.arrow.right")
                        }.font(.system(size: 14, weight: .semibold)).foregroundStyle(RevolutTokens.revBrand).padding(.top, 4)
                    }.frame(maxWidth: .infinity, alignment: .leading)
                    RoundedRectangle(cornerRadius: 16).fill(LinearGradient(colors: [RevolutTokens.revBrand, RevolutTokens.revBrand.opacity(0.6)], startPoint: .topLeading, endPoint: .bottomTrailing))
                        .frame(height: 180).overlay(alignment: .bottomLeading) {
                            VStack(alignment: .leading) { Text("Carte Virtuelle").font(.system(size: 13)).foregroundStyle(.white.opacity(0.8)); Text("•••• 4829").font(.system(size: 22, weight: .bold)).foregroundStyle(.white) }.padding(20)
                        }
                    VStack(alignment: .leading, spacing: 12) {
                        Text("Transactions").font(.system(size: 18, weight: .bold))
                        ForEach(RevolutDemoData.transactions) { tx in RevolutTransactionRow(tx: tx) }
                    }
                }.padding()
            }.background(RevolutTokens.revCanvas.ignoresSafeArea()).navigationTitle("Revolut")
        }
    }
}

private struct RevolutTransactionRow: View {
    let tx: RevolutTransaction
    var body: some View {
        HStack(spacing: 12) {
            Circle().fill(tx.color.opacity(0.2)).frame(width: 44, height: 44).overlay(Image(systemName: tx.icon).foregroundStyle(tx.color))
            VStack(alignment: .leading) { Text(tx.title).font(.system(size: 16, weight: .medium)); Text(tx.date).font(.system(size: 13)).foregroundStyle(.secondary) }
            Spacer()
            Text(tx.amount).font(.system(size: 16, weight: .semibold)).foregroundStyle(tx.amount.hasPrefix("-") ? .primary : .green)
        }
    }
}

private struct RevolutCardsScreen: View {
    var body: some View { NavigationStack { Text("Gérez vos cartes").padding().navigationTitle("Cartes") } }
}

private struct RevolutMoreScreen: View {
    var body: some View {
        NavigationStack { List { Section("Compte") { Label("Profil", systemImage: "person"); Label("Sécurité", systemImage: "lock") } }.navigationTitle("Plus") }
    }
}

private struct RevolutTransaction: Identifiable { let id = UUID(); let title: String; let date: String; let amount: String; let icon: String; let color: Color }
private enum RevolutDemoData {
    static let transactions: [RevolutTransaction] = [
        .init(title: "Monoprix", date: "Aujourd'hui", amount: "-24,80 €", icon: "cart.fill", color: .orange),
        .init(title: "Virement reçu", date: "Hier", amount: "+150,00 €", icon: "arrow.down.left", color: .green),
        .init(title: "Netflix", date: "28 juin", amount: "-13,49 €", icon: "play.tv.fill", color: .red),
    ]
}

private struct RevolutGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(RevolutTokens.revBrand.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}
