import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredGemini`).
struct LpspVendoredGeminiRootView: View {
    var body: some View {
        VendoredGeminiContentView()
    }
}
