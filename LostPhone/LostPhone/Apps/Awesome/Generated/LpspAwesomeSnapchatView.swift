import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/social/snapchat/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeSnapchatView: View {
    var body: some View {
        TabView {
            SnapchatTabHost(title: "Accueil", icon: "house.fill") { SnapchatScreenFactory.screen(for: "Accueil") }
                .tabItem { Label("Accueil", systemImage: "house.fill") }
            SnapchatTabHost(title: "Explorer", icon: "magnifyingglass") { SnapchatScreenFactory.screen(for: "Explorer") }
                .tabItem { Label("Explorer", systemImage: "magnifyingglass") }
            SnapchatTabHost(title: "Reels", icon: "play.rectangle") { SnapchatScreenFactory.screen(for: "Reels") }
                .tabItem { Label("Reels", systemImage: "play.rectangle") }
            SnapchatTabHost(title: "Profil", icon: "person.circle") { SnapchatScreenFactory.screen(for: "Profil") }
                .tabItem { Label("Profil", systemImage: "person.circle") }
        }
        .tint(SnapchatTokens.snapChatBlue)
    }
}

private struct SnapchatTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum SnapchatScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("feed") || t.contains("notification"):
            SnapchatFeedScreen()
        case let t where t.contains("explor") || t.contains("search") || t.contains("commun"):
            SnapchatExploreScreen()
        case let t where t.contains("reel") || t.contains("short") || t.contains("create") || t.contains("post"):
            SnapchatReelsScreen()
        case let t where t.contains("profil") || t.contains("profile") || t.contains("network"):
            SnapchatProfileScreen()
        default:
            SnapchatGenericListScreen(title: tab)
        }
    }
}
private enum SnapchatTokens {
    static let snapCanvas = Color.black                                   // #000000
    static let snapSurface1 = Color(red: 0.102, green: 0.102, blue: 0.102) // #1A1A1A
    static let snapSurface2 = Color(red: 0.173, green: 0.173, blue: 0.173) // #2C2C2C
    static let snapDivider = Color(red: 0.200, green: 0.200, blue: 0.200) // #333333
    static let snapLightCanvas = Color.white                                   // #FFFFFF
    static let snapLightSurface1 = Color(red: 0.949, green: 0.949, blue: 0.949) // #F2F2F2
    static let snapTextPrimary = Color.white                                   // #FFFFFF
    static let snapTextPrimaryLight = Color.black                                  // #000000
    static let snapTextSecondary = Color(red: 0.541, green: 0.541, blue: 0.561) // #8A8A8F
    static let snapTextTertiary = Color(red: 0.333, green: 0.333, blue: 0.333) // #555555
    static let snapYellow = Color(red: 1.000, green: 0.988, blue: 0.000) // #FFFC00
    static let snapYellowPressed = Color(red: 0.902, green: 0.890, blue: 0.000) // #E6E300
    static let snapPhotoRed = Color(red: 1.000, green: 0.180, blue: 0.239) // #FF2E3D
    static let snapVideoPurple = Color(red: 0.608, green: 0.318, blue: 1.000) // #9B51FF
    static let snapChatBlue = Color(red: 0.302, green: 0.655, blue: 1.000) // #4DA7FF
    static let snapAudioGreen = Color(red: 0.298, green: 0.851, blue: 0.392) // #4CD964
    static let snapErrorRed = Color(red: 1.000, green: 0.231, blue: 0.188) // #FF3B30
    static let snapSuccessGreen = Color(red: 0.000, green: 0.847, blue: 0.451) // #00D873
    static let snapLiveRed = Color(red: 1.000, green: 0.180, blue: 0.239) // #FF2E3D
}

private enum SnapchatFonts {
    static let snapSplashTitle = Font.system(size: 48, weight: .regular)
    static let snapScreenTitle = Font.system(size: 24, weight: .regular)
    static let snapSectionHeader = Font.system(size: 20, weight: .regular)
    static let snapChatRowName = Font.system(size: 16, weight: .regular)
    static let snapChatMessage = Font.system(size: 16, weight: .regular)
    static let snapChatStatus = Font.system(size: 13, weight: .regular)
    static let snapStoryName = Font.system(size: 14, weight: .regular)
    static let snapTimestamp = Font.system(size: 12, weight: .regular)
    static let snapStreakCount = Font.system(size: 18, weight: .regular)
    static let snapHudLabel = Font.system(size: 13, weight: .regular)
    static let snapButton = Font.system(size: 16, weight: .regular)
    static let snapLensLabel = Font.system(size: 13, weight: .regular)
    static let snapBitmojiCallout = Font.system(size: 14, weight: .regular)
    static let snapSpotlightCap = Font.system(size: 14, weight: .regular)
    static let snapDiscoverTitle = Font.system(size: 16, weight: .regular)
}
private struct SnapchatFeedScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 0) {
                    SnapchatStoriesBar()
                    ForEach(SnapchatDemoData.posts) { post in
                        SnapchatPostCard(post: post, accent: SnapchatTokens.snapChatBlue, textSec: SnapchatTokens.snapTextSecondary)
                        Divider().background(SnapchatTokens.snapTextSecondary.opacity(0.3))
                    }
                }
            }
            .background(SnapchatTokens.snapCanvas.ignoresSafeArea())
            .navigationTitle("Snapchat")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .topBarLeading) { Image(systemName: "camera").foregroundStyle(SnapchatTokens.snapChatBlue) }
                ToolbarItem(placement: .topBarTrailing) {
                    HStack(spacing: 16) {
                        Image(systemName: "heart")
                        Image(systemName: "paperplane")
                    }
                }
            }
        }
    }
}

private struct SnapchatStoriesBar: View {
    private let stories = SnapchatDemoData.stories
    var body: some View {
        ScrollView(.horizontal, showsIndicators: false) {
            HStack(spacing: 14) {
                ForEach(stories) { story in
                    VStack(spacing: 4) {
                        ZStack {
                            Circle().strokeBorder(
                                AngularGradient(colors: [.orange, .pink, .purple, .blue, .orange], center: .center),
                                lineWidth: story.unread ? 2.5 : 1
                            ).frame(width: 68, height: 68)
                            Circle().fill(story.color.gradient).frame(width: 60, height: 60)
                                .overlay(Text(story.initials).font(.system(size: 20, weight: .semibold)).foregroundStyle(.white))
                        }
                        Text(story.name).font(.system(size: 11)).lineLimit(1).frame(width: 72)
                    }
                }
            }
            .padding(.horizontal, 12).padding(.vertical, 10)
        }
    }
}

private struct SnapchatPostCard: View {
    let post: SnapchatPost
    let accent: Color
    let textSec: Color
    @State private var liked = false
    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            HStack {
                Circle().fill(post.avatarColor.gradient).frame(width: 32, height: 32)
                    .overlay(Text(post.initials).font(.system(size: 12, weight: .bold)).foregroundStyle(.white))
                Text(post.username).font(SnapchatFonts.snapScreenTitle)
                Spacer()
                Image(systemName: "ellipsis")
            }
            .padding(.horizontal, 12)
            RoundedRectangle(cornerRadius: 0).fill(post.avatarColor.opacity(0.25)).frame(height: 320)
                .overlay { Image(systemName: "photo").font(.system(size: 48)).foregroundStyle(textSec) }
            HStack(spacing: 16) {
                Button { liked.toggle() } label: { Image(systemName: liked ? "heart.fill" : "heart").foregroundStyle(liked ? .red : .primary) }
                Image(systemName: "bubble.right")
                Image(systemName: "paperplane")
                Spacer()
                Image(systemName: "bookmark")
            }
            .font(.system(size: 22)).padding(.horizontal, 12)
            Text("\(post.likes + (liked ? 1 : 0)) J'aime").font(.system(size: 14, weight: .semibold)).padding(.horizontal, 12)
            Text(post.caption).font(.system(size: 14)).padding(.horizontal, 12).padding(.bottom, 12)
        }
    }
}

private struct SnapchatExploreScreen: View {
    let cols = [GridItem(.flexible()), GridItem(.flexible()), GridItem(.flexible())]
    var body: some View {
        NavigationStack {
            ScrollView {
                LazyVGrid(columns: cols, spacing: 2) {
                    ForEach(0..<18, id: \.self) { i in
                        RoundedRectangle(cornerRadius: 2).fill(Color(hue: Double(i) / 18, saturation: 0.3, brightness: 0.85))
                            .aspectRatio(1, contentMode: .fit)
                    }
                }
            }
            .navigationTitle("Explorer")
        }
    }
}

private struct SnapchatReelsScreen: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            VStack {
                Spacer()
                Image(systemName: "play.rectangle.fill").font(.system(size: 64)).foregroundStyle(.white.opacity(0.8))
                Text("Reels").font(.title2.bold()).foregroundStyle(.white)
                Text("Faites défiler pour découvrir").foregroundStyle(.white.opacity(0.7))
                Spacer()
            }
        }
    }
}

private struct SnapchatProfileScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 16) {
                    HStack(spacing: 24) {
                        Circle().fill(SnapchatTokens.snapChatBlue.gradient).frame(width: 86, height: 86)
                            .overlay(Text("LP").font(.title.bold()).foregroundStyle(.white))
                        HStack(spacing: 20) {
                            SnapchatStatBlock(value: "128", label: "Publications")
                            SnapchatStatBlock(value: "1,2k", label: "Abonnés")
                            SnapchatStatBlock(value: "342", label: "Abonnements")
                        }
                    }
                    .padding(.horizontal)
                    Text("Joueur Lost Phone").font(.system(size: 14, weight: .semibold))
                    Text("Paris · Photographie · Voyage").font(.system(size: 14)).foregroundStyle(.secondary)
                    HStack {
                        Button {} label: { Text("Modifier le profil").font(.system(size: 14, weight: .semibold)).frame(maxWidth: .infinity).padding(.vertical, 8).background(RoundedRectangle(cornerRadius: 8).fill(SnapchatTokens.snapTextSecondary.opacity(0.15))) }
                        Button {} label: { Text("Partager").font(.system(size: 14, weight: .semibold)).frame(maxWidth: .infinity).padding(.vertical, 8).background(RoundedRectangle(cornerRadius: 8).fill(SnapchatTokens.snapTextSecondary.opacity(0.15))) }
                    }
                    .padding(.horizontal)
                }
                .padding(.top)
            }
            .navigationTitle("Profil")
        }
    }
}

private struct SnapchatStatBlock: View {
    let value: String; let label: String
    var body: some View {
        VStack { Text(value).font(.system(size: 16, weight: .bold)); Text(label).font(.system(size: 12)) }
    }
}

private struct SnapchatStory: Identifiable { let id = UUID(); let name: String; let initials: String; let color: Color; let unread: Bool }
private struct SnapchatPost: Identifiable { let id = UUID(); let username: String; let initials: String; let avatarColor: Color; let caption: String; let likes: Int }

private enum SnapchatDemoData {
    static let stories: [SnapchatStory] = [
        .init(name: "Votre story", initials: "+", color: .gray, unread: false),
        .init(name: "Camille", initials: "C", color: .pink, unread: true),
        .init(name: "Lucas", initials: "L", color: .blue, unread: true),
        .init(name: "Inès", initials: "I", color: .purple, unread: false),
    ]
    static let posts: [SnapchatPost] = [
        .init(username: "camille_p", initials: "C", avatarColor: .pink, caption: "Coucher de soleil sur la Seine 🌅 #paris", likes: 842),
        .init(username: "lucas.mrt", initials: "L", avatarColor: .blue, caption: "Nouveau café du quartier — à tester !", likes: 156),
    ]
}

private struct SnapchatGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(SnapchatTokens.snapChatBlue.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}

