import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/travel/uber/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeUberView: View {
    var body: some View {
        TabView {
            UberTabHost(title: "Home", icon: "house.fill") { UberScreenFactory.screen(for: "Home") }
                .tabItem { Label("Home", systemImage: "house.fill") }
            UberTabHost(title: "Services", icon: "square.grid.2x2.fill") { UberScreenFactory.screen(for: "Services") }
                .tabItem { Label("Services", systemImage: "square.grid.2x2.fill") }
            UberTabHost(title: "Activity", icon: "clock.fill") { UberScreenFactory.screen(for: "Activity") }
                .tabItem { Label("Activity", systemImage: "clock.fill") }
            UberTabHost(title: "Account", icon: "person.fill") { UberScreenFactory.screen(for: "Account") }
                .tabItem { Label("Account", systemImage: "person.fill") }
        }
        .tint(UberTokens.uberBlack)
    }
}

private struct UberTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum UberScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("service") || t.contains("contribute"):
            UberMapScreen()
        case let t where t.contains("activ") || t.contains("clock"):
            UberActivityScreen()
        default:
            UberGenericListScreen(title: tab)
        }
    }
}
private enum UberTokens {
    static let uberBlack = Color.black                                 // #000000
    static let uberWhite = Color.white                                 // #FFFFFF
    static let uberCanvasDark = Color(red: 0.047, green: 0.047, blue: 0.047) // #0C0C0C
    static let uberGray50 = Color(red: 0.965, green: 0.965, blue: 0.965) // #F6F6F6
    static let uberGray100 = Color(red: 0.933, green: 0.933, blue: 0.933) // #EEEEEE
    static let uberGray200 = Color(red: 0.898, green: 0.898, blue: 0.898) // #E5E5E5
    static let uberGray400 = Color(red: 0.686, green: 0.686, blue: 0.686) // #AFAFAF
    static let uberGray600 = Color(red: 0.459, green: 0.459, blue: 0.459) // #757575
    static let uberGray700 = Color(red: 0.329, green: 0.329, blue: 0.329) // #545454
    static let uberGray900 = Color(red: 0.184, green: 0.184, blue: 0.184) // #2F2F2F
    static let uberGray950 = Color(red: 0.102, green: 0.102, blue: 0.102) // #1A1A1A
    static let uberGreen = Color(red: 0.020, green: 0.639, blue: 0.341) // #05A357
    static let uberRed = Color(red: 0.843, green: 0.129, blue: 0.075) // #D72113
    static let uberBlue = Color(red: 0.039, green: 0.278, blue: 1.000) // #0A47FF
    static let uberAmber = Color(red: 1.000, green: 0.796, blue: 0.000) // #FFCB00
    static let uberSurface = Color("UberSurface")      // #FFFFFF / #1A1A1A
    static let uberSurfaceAlt = Color("UberSurfaceAlt")   // #F6F6F6 / #2F2F2F
    static let uberTextPrimary = Color("UberTextPrimary")  // #000000 / #FFFFFF
    static let uberTextSecondary = Color("UberTextSecondary") // #757575 / #AFAFAF
    static let uberDivider = Color("UberDivider")      // #E5E5E5 / #3A3A3A
}

private enum UberFonts {
    static let uberHero = Font.system(size: 36, weight: .regular)
    static let uberSheetTitle = Font.system(size: 24, weight: .regular)
    static let uberNavTitle = Font.system(size: 18, weight: .regular)
    static let uberWhereTo = Font.system(size: 18, weight: .regular)
    static let uberRowTitle = Font.system(size: 16, weight: .regular)
    static let uberBody = Font.system(size: 15, weight: .regular)
    static let uberMeta = Font.system(size: 14, weight: .regular)
    static let uberLabelUpper = Font.system(size: 11, weight: .regular)
    static let uberButton = Font.system(size: 17, weight: .regular)
    static let uberButtonSmall = Font.system(size: 15, weight: .regular)
    static let uberTab = Font.system(size: 11, weight: .regular)
    static let uberCaption = Font.system(size: 12, weight: .regular)
    static let uberPrice = Font.system(size: 16, weight: .regular)
    static let uberETA = Font.system(size: 14, weight: .regular)
    static let uberETABadge = Font.system(size: 18, weight: .regular)
    static let uberAddress = Font.system(size: 13, weight: .regular)
}
private struct UberMapScreen: View {
    var body: some View {
        ZStack(alignment: .bottom) {
            UberMapPlaceholder()
            UberRideSheet(accent: UberTokens.uberBlack, surface: UberTokens.uberSurface)
        }.ignoresSafeArea(edges: .bottom)
    }
}

private struct UberMapPlaceholder: View {
    var body: some View {
        ZStack {
            LinearGradient(colors: [Color(red: 0.85, green: 0.92, blue: 0.88), Color(red: 0.75, green: 0.85, blue: 0.78)], startPoint: .top, endPoint: .bottom)
            Image(systemName: "map.fill").font(.system(size: 120)).foregroundStyle(.white.opacity(0.4))
            VStack {
                HStack { Image(systemName: "location.fill"); Text("Place de la République") }.padding(10).background(.ultraThinMaterial).clipShape(Capsule()).padding()
                Spacer()
            }
        }
    }
}

private struct UberRideSheet: View {
    let accent: Color; let surface: Color
    var body: some View {
        VStack(spacing: 0) {
            Capsule().fill(Color.secondary.opacity(0.4)).frame(width: 36, height: 5).padding(.top, 8)
            VStack(alignment: .leading, spacing: 16) {
                Text("Où allez-vous ?").font(.headline)
                Text("République → Gare de Lyon").foregroundStyle(.secondary)
                ForEach(UberDemoData.rides) { ride in
                    HStack {
                        Image(systemName: ride.icon).font(.title2).frame(width: 40)
                        VStack(alignment: .leading) { Text(ride.name).font(.headline); Text(ride.eta).font(.subheadline).foregroundStyle(.secondary) }
                        Spacer(); Text(ride.price).font(.headline)
                    }.padding().background(RoundedRectangle(cornerRadius: 12).fill(surface))
                }
                Button("Confirmer Uber") {}.frame(maxWidth: .infinity).padding().background(RoundedRectangle(cornerRadius: 10).fill(accent)).foregroundStyle(.white).font(.headline)
            }.padding()
        }.background(RoundedRectangle(cornerRadius: 16).fill(.regularMaterial))
    }
}

private struct UberActivityScreen: View {
    var body: some View {
        NavigationStack {
            List(UberDemoData.rides) { r in
                HStack { Image(systemName: r.icon); VStack(alignment: .leading) { Text(r.name); Text(r.eta).foregroundStyle(.secondary) }; Spacer(); Text(r.price) }
            }.navigationTitle("Activité")
        }
    }
}

private struct UberRide: Identifiable { let id = UUID(); let name: String; let eta: String; let price: String; let icon: String }
private enum UberDemoData {
    static let rides: [UberRide] = [
        .init(name: "UberX", eta: "3 min", price: "12,40 €", icon: "car.fill"),
        .init(name: "Comfort", eta: "5 min", price: "16,80 €", icon: "car.side.fill"),
        .init(name: "Green", eta: "4 min", price: "13,20 €", icon: "leaf.fill"),
    ]
}

private struct UberGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(UberTokens.uberBlack.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}
