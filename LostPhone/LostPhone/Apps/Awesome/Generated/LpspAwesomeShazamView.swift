import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/music/shazam/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeShazamView: View {
    var body: some View {
        TabView {
            ShazamTabHost(title: "Découvrir", icon: "waveform") { ShazamScreenFactory.screen(for: "Découvrir") }
                .tabItem { Label("Découvrir", systemImage: "waveform") }
        }
        .tint(ShazamTokens.shazamBlue)
    }
}

private struct ShazamTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum ShazamScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("découvr") || t.contains("discover") || t.contains("home"):
            ShazamListenScreen()
        default:
            ShazamGenericListScreen(title: tab)
        }
    }
}
private enum ShazamTokens {
    static let shazamCore = Color(red: 0.0,   green: 0.314, blue: 1.0)   // #0050FF
    static let shazamBlue = Color(red: 0.0,   green: 0.533, blue: 1.0)   // #0088FF
    static let shazamSpace = Color(red: 0.031, green: 0.035, blue: 0.055) // #08090E
    static let shazamBluePressed = Color(red: 0.0, green: 0.435, blue: 0.878) // #006FE0
    static let shazamTextPrimary = Color.white                                 // #FFFFFF
    static let shazamTextSecondary = Color(red: 0.722, green: 0.769, blue: 1.0)   // #B8C4FF periwinkle
    static let shazamTextTertiary = Color(red: 0.722, green: 0.769, blue: 1.0).opacity(0.55)
    static let shazamGlass = Color.white.opacity(0.08)
    static let shazamGlassStrong = Color.white.opacity(0.14)
    static let shazamDivider = Color.white.opacity(0.12)
    static let appleMusicPink = Color(red: 0.980, green: 0.141, blue: 0.235) // #FA243C
    static let shazamErrorRed = Color(red: 1.0,   green: 0.271, blue: 0.227) // #FF453A
}

private enum ShazamFonts {
    static let shazamResultLarge = Font.system(size: 28, weight: .regular)
    static let shazamResult = Font.system(size: 22, weight: .regular)
    static let shazamSection = Font.system(size: 20, weight: .regular)
    static let shazamPrompt = Font.system(size: 18, weight: .regular)
    static let shazamCardTitle = Font.system(size: 16, weight: .regular)
    static let shazamSubtitle = Font.system(size: 14, weight: .regular)
    static let shazamBody = Font.system(size: 15, weight: .regular)
    static let shazamMeta = Font.system(size: 13, weight: .regular)
    static let shazamLabelUpper = Font.system(size: 11, weight: .regular)
    static let shazamButton = Font.system(size: 16, weight: .regular)
    static let shazamButtonSec = Font.system(size: 14, weight: .regular)
}
private struct ShazamListenScreen: View {
    @State private var listening = false
    var body: some View {
        ZStack {
            ShazamTokens.shazamCore.ignoresSafeArea()
            VStack(spacing: 32) {
                Spacer()
                Text(listening ? "Écoute en cours…" : "Appuyez pour identifier").font(.title3).foregroundStyle(.secondary)
                Button { listening.toggle() } label: {
                    ZStack {
                        Circle().fill(ShazamTokens.shazamBlue.gradient).frame(width: 180, height: 180)
                        Image(systemName: "shazam.logo.fill").font(.system(size: 64)).foregroundStyle(.white)
                    }
                }
                if listening { ShazamWaveform(accent: ShazamTokens.shazamBlue) }
                Spacer()
                Text("Dernière identification : Édith Piaf — La Vie en Rose").font(.footnote).foregroundStyle(.secondary).padding()
            }
        }
    }
}

private struct ShazamWaveform: View {
    let accent: Color
    var body: some View {
        HStack(spacing: 4) {
            ForEach(0..<24, id: \.self) { i in
                RoundedRectangle(cornerRadius: 2).fill(accent).frame(width: 4, height: CGFloat([12, 28, 18, 36, 22, 40, 16, 32][i % 8]))
            }
        }.frame(height: 44)
    }
}

private struct ShazamGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(ShazamTokens.shazamBlue.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}
