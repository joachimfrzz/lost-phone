import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/travel/google-maps/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomePlansView: View {
    var body: some View {
        TabView {
            PlansTabHost(title: "Contribute", icon: "plus.circle.fill") { PlansScreenFactory.screen(for: "Contribute") }
                .tabItem { Label("Contribute", systemImage: "plus.circle.fill") }
        }
        .tint(PlansTokens.gmTextPrimary)
    }
}

private struct PlansTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum PlansScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("service") || t.contains("contribute"):
            PlansMapScreen()
        case let t where t.contains("activ") || t.contains("clock"):
            PlansActivityScreen()
        default:
            PlansGenericListScreen(title: tab)
        }
    }
}
private enum PlansTokens {
    static let gmCanvas = white                                   // #FFFFFF
    static let gmSurfaceMuted = Color(red: 0.945, green: 0.953, blue: 0.957) // #F1F3F4
    static let gmDivider = Color(red: 0.855, green: 0.863, blue: 0.878) // #DADCE0
    static let gmTextPrimary = Color(red: 0.125, green: 0.133, blue: 0.141) // #202124
    static let gmTextSecondary = Color(red: 0.373, green: 0.388, blue: 0.408) // #5F6368
    static let gmTextTertiary = Color(red: 0.502, green: 0.525, blue: 0.545) // #80868B
    static let gmBlue = Color(red: 0.259, green: 0.522, blue: 0.957) // #4285F4
    static let gmBluePressed = Color(red: 0.102, green: 0.451, blue: 0.910) // #1A73E8
    static let gmBlueDark = Color(red: 0.090, green: 0.306, blue: 0.651) // #174EA6
    static let gmRed = Color(red: 0.918, green: 0.263, blue: 0.208) // #EA4335
    static let gmYellow = Color(red: 0.984, green: 0.737, blue: 0.016) // #FBBC04
    static let gmGreen = Color(red: 0.204, green: 0.659, blue: 0.325) // #34A853
    static let gmOrange = Color(red: 0.984, green: 0.549, blue: 0.000) // #FB8C00
    static let gmRoadWhite = white                                   // #FFFFFF
    static let gmHighwayYellow = Color(red: 0.992, green: 0.965, blue: 0.890) // #FDF6E3
    static let gmWaterBlue = Color(red: 0.667, green: 0.855, blue: 1.000) // #AADAFF
    static let gmParkGreen = Color(red: 0.784, green: 0.902, blue: 0.788) // #C8E6C9
    static let gmBuildingFill = Color(red: 0.941, green: 0.941, blue: 0.941) // #F0F0F0
    static let gmDarkCanvas = Color(red: 0.125, green: 0.133, blue: 0.141) // #202124
    static let gmDarkSurface1 = Color(red: 0.176, green: 0.180, blue: 0.192) // #2D2E31
    static let gmDarkSurface2 = Color(red: 0.235, green: 0.251, blue: 0.263) // #3C4043
    static let gmDarkTextPrim = Color(red: 0.910, green: 0.918, blue: 0.929) // #E8EAED
    static let gmBlueHalo = Color(red: 0.259, green: 0.522, blue: 0.957, opacity: 0.18)
    static let gmBlueHaloEdge = Color(red: 0.259, green: 0.522, blue: 0.957, opacity: 0.4)
}

private enum PlansFonts {
    static let gmNavTurn = Font.system(size: 36, weight: .regular)
    static let gmScreenTitle = Font.system(size: 28, weight: .regular)
    static let gmPlaceTitle = Font.system(size: 20, weight: .regular)
    static let gmSection = Font.system(size: 16, weight: .regular)
    static let gmButton = Font.system(size: 16, weight: .regular)
    static let gmButtonSmall = Font.system(size: 14, weight: .regular)
    static let gmTab = Font.system(size: 11, weight: .regular)
    static let gmChip = Font.system(size: 14, weight: .regular)
    static let gmRowTitle = Font.system(size: 16, weight: .regular)
    static let gmBody = Font.system(size: 14, weight: .regular)
    static let gmAddress = Font.system(size: 14, weight: .regular)
    static let gmMeta = Font.system(size: 13, weight: .regular)
    static let gmRating = Font.system(size: 14, weight: .regular)
    static let gmETA = Font.system(size: 16, weight: .regular)
    static let gmDistancePill = Font.system(size: 13, weight: .regular)
}
private struct PlansMapScreen: View {
    var body: some View {
        ZStack(alignment: .bottom) {
            PlansMapPlaceholder()
            PlansRideSheet(accent: PlansTokens.gmTextPrimary, surface: PlansTokens.gmSurfaceMuted)
        }.ignoresSafeArea(edges: .bottom)
    }
}

private struct PlansMapPlaceholder: View {
    var body: some View {
        ZStack {
            LinearGradient(colors: [Color(red: 0.85, green: 0.92, blue: 0.88), Color(red: 0.75, green: 0.85, blue: 0.78)], startPoint: .top, endPoint: .bottom)
            Image(systemName: "map.fill").font(.system(size: 120)).foregroundStyle(.white.opacity(0.4))
            VStack {
                HStack { Image(systemName: "location.fill"); Text("Place de la République") }.padding(10).background(.ultraThinMaterial).clipShape(Capsule()).padding()
                Spacer()
            }
        }
    }
}

private struct PlansRideSheet: View {
    let accent: Color; let surface: Color
    var body: some View {
        VStack(spacing: 0) {
            Capsule().fill(Color.secondary.opacity(0.4)).frame(width: 36, height: 5).padding(.top, 8)
            VStack(alignment: .leading, spacing: 16) {
                Text("Où allez-vous ?").font(.headline)
                Text("République → Gare de Lyon").foregroundStyle(.secondary)
                ForEach(PlansDemoData.rides) { ride in
                    HStack {
                        Image(systemName: ride.icon).font(.title2).frame(width: 40)
                        VStack(alignment: .leading) { Text(ride.name).font(.headline); Text(ride.eta).font(.subheadline).foregroundStyle(.secondary) }
                        Spacer(); Text(ride.price).font(.headline)
                    }.padding().background(RoundedRectangle(cornerRadius: 12).fill(surface))
                }
                Button("Confirmer Plans") {}.frame(maxWidth: .infinity).padding().background(RoundedRectangle(cornerRadius: 10).fill(accent)).foregroundStyle(.white).font(.headline)
            }.padding()
        }.background(RoundedRectangle(cornerRadius: 16).fill(.regularMaterial))
    }
}

private struct PlansActivityScreen: View {
    var body: some View {
        NavigationStack {
            List(PlansDemoData.rides) { r in
                HStack { Image(systemName: r.icon); VStack(alignment: .leading) { Text(r.name); Text(r.eta).foregroundStyle(.secondary) }; Spacer(); Text(r.price) }
            }.navigationTitle("Activité")
        }
    }
}

private struct PlansRide: Identifiable { let id = UUID(); let name: String; let eta: String; let price: String; let icon: String }
private enum PlansDemoData {
    static let rides: [PlansRide] = [
        .init(name: "UberX", eta: "3 min", price: "12,40 €", icon: "car.fill"),
        .init(name: "Comfort", eta: "5 min", price: "16,80 €", icon: "car.side.fill"),
        .init(name: "Green", eta: "4 min", price: "13,20 €", icon: "leaf.fill"),
    ]
}

private struct PlansGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(PlansTokens.gmTextPrimary.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}
