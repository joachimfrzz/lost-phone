import SwiftUI

struct VendoredSpotifyCreateView: View {
    var body: some View {
        ZStack {
            Color.spotifyBlack.ignoresSafeArea()
            
            Text("Create")
                .foregroundColor(.spotifyWhite)
        }
    }
}
