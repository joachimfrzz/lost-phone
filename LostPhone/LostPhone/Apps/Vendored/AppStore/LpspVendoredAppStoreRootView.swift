import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredAppStore`).
struct LpspVendoredAppStoreRootView: View {
    var body: some View {
        VendoredAppStoreContentView()
    }
}
