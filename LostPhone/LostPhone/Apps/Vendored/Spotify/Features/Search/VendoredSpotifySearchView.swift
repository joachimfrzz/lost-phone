import SwiftUI

struct VendoredSpotifySearchView: View {
    var body: some View {
        ZStack {
            Color.spotifyBlack.ignoresSafeArea()
            
            Text("Search")
                .foregroundColor(.spotifyWhite)
        }
    }
}
