{
  "name": "lost-phone-studio",
  "private": true,
  "version": "0.2.0",
  "description": "LostPhone Studio — tooling minimal (LPSP sync). Legacy React/Capacitor → archive/legacy-react/",
  "type": "module",
  "scripts": {
    "lpsp:sync": "node scripts/sync-lpsp-swift.mjs"
  }
}
