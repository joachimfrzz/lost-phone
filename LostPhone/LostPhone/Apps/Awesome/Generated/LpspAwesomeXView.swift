import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/social/x-twitter/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeXView: View {
    var body: some View {
        TabView {
            XTabHost(title: "Accueil", icon: "house.fill") { XScreenFactory.screen(for: "Accueil") }
                .tabItem { Label("Accueil", systemImage: "house.fill") }
            XTabHost(title: "Explorer", icon: "magnifyingglass") { XScreenFactory.screen(for: "Explorer") }
                .tabItem { Label("Explorer", systemImage: "magnifyingglass") }
            XTabHost(title: "Reels", icon: "play.rectangle") { XScreenFactory.screen(for: "Reels") }
                .tabItem { Label("Reels", systemImage: "play.rectangle") }
            XTabHost(title: "Profil", icon: "person.circle") { XScreenFactory.screen(for: "Profil") }
                .tabItem { Label("Profil", systemImage: "person.circle") }
        }
        .tint(XTokens.xBlue)
    }
}

private struct XTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum XScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("feed") || t.contains("notification"):
            XFeedScreen()
        case let t where t.contains("explor") || t.contains("search") || t.contains("commun"):
            XExploreScreen()
        case let t where t.contains("reel") || t.contains("short") || t.contains("create") || t.contains("post"):
            XReelsScreen()
        case let t where t.contains("profil") || t.contains("profile") || t.contains("network"):
            XProfileScreen()
        default:
            XGenericListScreen(title: tab)
        }
    }
}
private enum XTokens {
    static let xCanvas = black                                   // #000000
    static let xSurface1 = Color(red: 0.086, green: 0.094, blue: 0.110) // #16181C
    static let xSurface2 = Color(red: 0.118, green: 0.125, blue: 0.141) // #1E2024
    static let xDivider = Color(red: 0.184, green: 0.200, blue: 0.212) // #2F3336
    static let xDimCanvas = Color(red: 0.082, green: 0.125, blue: 0.173) // #15202B
    static let xDimSurface1 = Color(red: 0.098, green: 0.153, blue: 0.204) // #192734
    static let xDimDivider = Color(red: 0.220, green: 0.267, blue: 0.302) // #38444D
    static let xLightCanvas = white                                  // #FFFFFF
    static let xLightSurface1 = Color(red: 0.969, green: 0.976, blue: 0.976) // #F7F9F9
    static let xLightSurface2 = Color(red: 0.937, green: 0.953, blue: 0.957) // #EFF3F4
    static let xTextPrimaryDark = Color(red: 0.906, green: 0.914, blue: 0.918) // #E7E9EA
    static let xTextPrimaryLight = Color(red: 0.059, green: 0.078, blue: 0.098) // #0F1419
    static let xTextSecondaryDark = Color(red: 0.443, green: 0.463, blue: 0.482) // #71767B
    static let xTextSecondaryLight = Color(red: 0.325, green: 0.392, blue: 0.443) // #536471
    static let xBlue = Color(red: 0.114, green: 0.608, blue: 0.941) // #1D9BF0
    static let xBluePressed = Color(red: 0.102, green: 0.549, blue: 0.847) // #1A8CD8
    static let xRepostGreen = Color(red: 0.000, green: 0.729, blue: 0.486) // #00BA7C
    static let xLikePink = Color(red: 0.976, green: 0.094, blue: 0.502) // #F91880
    static let xVerifiedGold = Color(red: 0.918, green: 0.702, blue: 0.031) // #EAB308
    static let xVerifiedGray = Color(red: 0.510, green: 0.604, blue: 0.671) // #829AAB
    static let xErrorRed = Color(red: 0.957, green: 0.129, blue: 0.180) // #F4212E
}

private enum XFonts {
    static let xScreenTitle = Font.system(size: 20, weight: .regular)
    static let xSectionHeader = Font.system(size: 17, weight: .regular)
    static let xDisplayName = Font.system(size: 15, weight: .regular)
    static let xPostBody = Font.system(size: 15, weight: .regular)
    static let xQuotedBody = Font.system(size: 14, weight: .regular)
    static let xHandle = Font.system(size: 15, weight: .regular)
    static let xActionCount = Font.system(size: 13, weight: .regular)
    static let xTrendingTopic = Font.system(size: 15, weight: .regular)
    static let xTrendingMeta = Font.system(size: 13, weight: .regular)
    static let xButton = Font.system(size: 15, weight: .regular)
    static let xDMBody = Font.system(size: 15, weight: .regular)
    static let xDMTimestamp = Font.system(size: 11, weight: .regular)
}
private struct XFeedScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 0) {
                    XStoriesBar()
                    ForEach(XDemoData.posts) { post in
                        XPostCard(post: post, accent: XTokens.xBlue, textSec: XTokens.xTextSecondaryDark)
                        Divider().background(XTokens.xTextSecondaryDark.opacity(0.3))
                    }
                }
            }
            .background(XTokens.xCanvas.ignoresSafeArea())
            .navigationTitle("X")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .topBarLeading) { Image(systemName: "camera").foregroundStyle(XTokens.xBlue) }
                ToolbarItem(placement: .topBarTrailing) {
                    HStack(spacing: 16) {
                        Image(systemName: "heart")
                        Image(systemName: "paperplane")
                    }
                }
            }
        }
    }
}

private struct XStoriesBar: View {
    private let stories = XDemoData.stories
    var body: some View {
        ScrollView(.horizontal, showsIndicators: false) {
            HStack(spacing: 14) {
                ForEach(stories) { story in
                    VStack(spacing: 4) {
                        ZStack {
                            Circle().strokeBorder(
                                AngularGradient(colors: [.orange, .pink, .purple, .blue, .orange], center: .center),
                                lineWidth: story.unread ? 2.5 : 1
                            ).frame(width: 68, height: 68)
                            Circle().fill(story.color.gradient).frame(width: 60, height: 60)
                                .overlay(Text(story.initials).font(.system(size: 20, weight: .semibold)).foregroundStyle(.white))
                        }
                        Text(story.name).font(.system(size: 11)).lineLimit(1).frame(width: 72)
                    }
                }
            }
            .padding(.horizontal, 12).padding(.vertical, 10)
        }
    }
}

private struct XPostCard: View {
    let post: XPost
    let accent: Color
    let textSec: Color
    @State private var liked = false
    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            HStack {
                Circle().fill(post.avatarColor.gradient).frame(width: 32, height: 32)
                    .overlay(Text(post.initials).font(.system(size: 12, weight: .bold)).foregroundStyle(.white))
                Text(post.username).font(XFonts.xScreenTitle)
                Spacer()
                Image(systemName: "ellipsis")
            }
            .padding(.horizontal, 12)
            RoundedRectangle(cornerRadius: 0).fill(post.avatarColor.opacity(0.25)).frame(height: 320)
                .overlay { Image(systemName: "photo").font(.system(size: 48)).foregroundStyle(textSec) }
            HStack(spacing: 16) {
                Button { liked.toggle() } label: { Image(systemName: liked ? "heart.fill" : "heart").foregroundStyle(liked ? .red : .primary) }
                Image(systemName: "bubble.right")
                Image(systemName: "paperplane")
                Spacer()
                Image(systemName: "bookmark")
            }
            .font(.system(size: 22)).padding(.horizontal, 12)
            Text("\(post.likes + (liked ? 1 : 0)) J'aime").font(.system(size: 14, weight: .semibold)).padding(.horizontal, 12)
            Text(post.caption).font(.system(size: 14)).padding(.horizontal, 12).padding(.bottom, 12)
        }
    }
}

private struct XExploreScreen: View {
    let cols = [GridItem(.flexible()), GridItem(.flexible()), GridItem(.flexible())]
    var body: some View {
        NavigationStack {
            ScrollView {
                LazyVGrid(columns: cols, spacing: 2) {
                    ForEach(0..<18, id: \.self) { i in
                        RoundedRectangle(cornerRadius: 2).fill(Color(hue: Double(i) / 18, saturation: 0.3, brightness: 0.85))
                            .aspectRatio(1, contentMode: .fit)
                    }
                }
            }
            .navigationTitle("Explorer")
        }
    }
}

private struct XReelsScreen: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            VStack {
                Spacer()
                Image(systemName: "play.rectangle.fill").font(.system(size: 64)).foregroundStyle(.white.opacity(0.8))
                Text("Reels").font(.title2.bold()).foregroundStyle(.white)
                Text("Faites défiler pour découvrir").foregroundStyle(.white.opacity(0.7))
                Spacer()
            }
        }
    }
}

private struct XProfileScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 16) {
                    HStack(spacing: 24) {
                        Circle().fill(XTokens.xBlue.gradient).frame(width: 86, height: 86)
                            .overlay(Text("LP").font(.title.bold()).foregroundStyle(.white))
                        HStack(spacing: 20) {
                            XStatBlock(value: "128", label: "Publications")
                            XStatBlock(value: "1,2k", label: "Abonnés")
                            XStatBlock(value: "342", label: "Abonnements")
                        }
                    }
                    .padding(.horizontal)
                    Text("Joueur Lost Phone").font(.system(size: 14, weight: .semibold))
                    Text("Paris · Photographie · Voyage").font(.system(size: 14)).foregroundStyle(.secondary)
                    HStack {
                        Button {} label: { Text("Modifier le profil").font(.system(size: 14, weight: .semibold)).frame(maxWidth: .infinity).padding(.vertical, 8).background(RoundedRectangle(cornerRadius: 8).fill(XTokens.xTextSecondaryDark.opacity(0.15))) }
                        Button {} label: { Text("Partager").font(.system(size: 14, weight: .semibold)).frame(maxWidth: .infinity).padding(.vertical, 8).background(RoundedRectangle(cornerRadius: 8).fill(XTokens.xTextSecondaryDark.opacity(0.15))) }
                    }
                    .padding(.horizontal)
                }
                .padding(.top)
            }
            .navigationTitle("Profil")
        }
    }
}

private struct XStatBlock: View {
    let value: String; let label: String
    var body: some View {
        VStack { Text(value).font(.system(size: 16, weight: .bold)); Text(label).font(.system(size: 12)) }
    }
}

private struct XStory: Identifiable { let id = UUID(); let name: String; let initials: String; let color: Color; let unread: Bool }
private struct XPost: Identifiable { let id = UUID(); let username: String; let initials: String; let avatarColor: Color; let caption: String; let likes: Int }

private enum XDemoData {
    static let stories: [XStory] = [
        .init(name: "Votre story", initials: "+", color: .gray, unread: false),
        .init(name: "Camille", initials: "C", color: .pink, unread: true),
        .init(name: "Lucas", initials: "L", color: .blue, unread: true),
        .init(name: "Inès", initials: "I", color: .purple, unread: false),
    ]
    static let posts: [XPost] = [
        .init(username: "camille_p", initials: "C", avatarColor: .pink, caption: "Coucher de soleil sur la Seine 🌅 #paris", likes: 842),
        .init(username: "lucas.mrt", initials: "L", avatarColor: .blue, caption: "Nouveau café du quartier — à tester !", likes: 156),
    ]
}

private struct XGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(XTokens.xBlue.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}

