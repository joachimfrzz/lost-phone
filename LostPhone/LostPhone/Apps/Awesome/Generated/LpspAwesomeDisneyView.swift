import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/video/disney-plus/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeDisneyView: View {
    var body: some View {
        TabView {
            DisneyTabHost(title: "Home", icon: "house.fill") { DisneyScreenFactory.screen(for: "Home") }
                .tabItem { Label("Home", systemImage: "house.fill") }
            DisneyTabHost(title: "Search", icon: "magnifyingglass") { DisneyScreenFactory.screen(for: "Search") }
                .tabItem { Label("Search", systemImage: "magnifyingglass") }
            DisneyTabHost(title: "Watchlist", icon: "plus.rectangle.on.rectangle") { DisneyScreenFactory.screen(for: "Watchlist") }
                .tabItem { Label("Watchlist", systemImage: "plus.rectangle.on.rectangle") }
            DisneyTabHost(title: "Downloads", icon: "arrow.down.circle") { DisneyScreenFactory.screen(for: "Downloads") }
                .tabItem { Label("Downloads", systemImage: "arrow.down.circle") }
            DisneyTabHost(title: "Profile", icon: "person.crop.circle") { DisneyScreenFactory.screen(for: "Profile") }
                .tabItem { Label("Profile", systemImage: "person.crop.circle") }
        }
        .tint(DisneyTokens.dpLiveRed)
    }
}

private struct DisneyTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum DisneyScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("watch"):
            DisneyHomeScreen()
        case let t where t.contains("search") || t.contains("recherch") || t.contains("new") || t.contains("hot") || t.contains("short"):
            DisneyBrowseScreen()
        case let t where t.contains("profil") || t.contains("profile") || t.contains("netflix") || t.contains("watchlist") || t.contains("store") || t.contains("download") || t.contains("notif"):
            DisneyProfilePickerScreen()
        default:
            DisneyGenericListScreen(title: tab)
        }
    }
}
private enum DisneyTokens {
    static let dpCanvas = Color(red: 0.039, green: 0.055, blue: 0.165) // #0A0E2A
    static let dpSurface1 = Color(red: 0.071, green: 0.082, blue: 0.180) // #12152E
    static let dpSurface2 = Color(red: 0.102, green: 0.122, blue: 0.239) // #1A1F3D
    static let dpDivider = Color(red: 0.165, green: 0.188, blue: 0.314) // #2A3050
    static let dpTextPrimary = Color.white                                 // #FFFFFF
    static let dpTextSecondary = Color(red: 0.627, green: 0.651, blue: 0.753) // #A0A6C0
    static let dpTextTertiary = Color(red: 0.353, green: 0.376, blue: 0.502) // #5A6080
    static let dpBlue = Color(red: 0.0,   green: 0.388, blue: 0.898) // #0063E5
    static let dpGlowBlue = Color(red: 0.102, green: 0.459, blue: 1.0)   // #1A75FF
    static let dpBluePressed = Color(red: 0.0,   green: 0.322, blue: 0.741) // #0052BD
    static let dpLiveRed = Color(red: 0.898, green: 0.282, blue: 0.302) // #E5484D
}

private enum DisneyFonts {
    static let dpBillboard = Font.system(size: 30, weight: .regular)
    static let dpDetailTitle = Font.system(size: 26, weight: .regular)
    static let dpSection = Font.system(size: 20, weight: .regular)
    static let dpRowHeader = Font.system(size: 18, weight: .regular)
    static let dpCardTitle = Font.system(size: 14, weight: .regular)
    static let dpSynopsis = Font.system(size: 15, weight: .regular)
    static let dpMetaStrip = Font.system(size: 13, weight: .regular)
    static let dpSubtitle = Font.system(size: 14, weight: .regular)
    static let dpMeta = Font.system(size: 12, weight: .regular)
    static let dpBadge = Font.system(size: 10, weight: .regular)
    static let dpButton = Font.system(size: 16, weight: .regular)
    static let dpButtonSec = Font.system(size: 14, weight: .regular)
    static let dpTab = Font.system(size: 10, weight: .regular)
}
private struct DisneyHomeScreen: View {
    var body: some View {
        NavigationStack {
            ZStack(alignment: .bottom) {
                ScrollView {
                    VStack(alignment: .leading, spacing: 20) {
                        DisneyHeroBillboard()
                        ForEach(DisneyDemoData.rows) { row in
                            DisneyPosterRow(title: row.title, items: row.items, accent: DisneyTokens.dpLiveRed)
                        }
                    }
                    .padding(.bottom, 24)
                }
                .background(DisneyTokens.dpCanvas.ignoresSafeArea())
            }
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .principal) { Text("Disney+").font(.system(size: 20, weight: .bold)).foregroundStyle(DisneyTokens.dpTextPrimary) }
                ToolbarItem(placement: .topBarTrailing) { Image(systemName: "person.crop.circle").foregroundStyle(DisneyTokens.dpTextPrimary) }
            }
            .toolbarBackground(DisneyTokens.dpCanvas, for: .navigationBar)
        }
    }
}

private struct DisneyHeroBillboard: View {
    var body: some View {
        ZStack(alignment: .bottomLeading) {
            RoundedRectangle(cornerRadius: 4).fill(
                LinearGradient(colors: [DisneyTokens.dpSurface1, .black.opacity(0.3)], startPoint: .top, endPoint: .bottom)
            ).aspectRatio(16/9, contentMode: .fit)
            VStack(alignment: .leading, spacing: 8) {
                Text("SÉRIE TENDANCE").font(.system(size: 11, weight: .bold)).foregroundStyle(DisneyTokens.dpTextSecondary)
                Text("L'Énigme de Belleville").font(.system(size: 28, weight: .bold)).foregroundStyle(.white)
                HStack(spacing: 12) {
                    Label("Lecture", systemImage: "play.fill").font(.system(size: 15, weight: .bold))
                        .frame(maxWidth: .infinity).padding(.vertical, 10)
                        .background(RoundedRectangle(cornerRadius: 4).fill(DisneyTokens.dpLiveRed))
                    Label("Plus d'infos", systemImage: "info.circle").font(.system(size: 15, weight: .semibold))
                        .frame(maxWidth: .infinity).padding(.vertical, 10)
                        .background(RoundedRectangle(cornerRadius: 4).fill(DisneyTokens.dpSurface1))
                }
            }
            .padding(16)
        }
        .padding(.horizontal, 12)
    }
}

private struct DisneyPosterRow: View {
    let title: String; let items: [String]; let accent: Color
    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            Text(title).font(.system(size: 17, weight: .bold)).foregroundStyle(.white).padding(.horizontal, 12)
            ScrollView(.horizontal, showsIndicators: false) {
                HStack(spacing: 8) {
                    ForEach(items, id: \.self) { item in
                        RoundedRectangle(cornerRadius: 4).fill(accent.opacity(0.35)).frame(width: 110, height: 160)
                            .overlay(alignment: .bottomLeading) {
                                Text(item).font(.system(size: 11, weight: .semibold)).foregroundStyle(.white).padding(6)
                            }
                    }
                }
                .padding(.horizontal, 12)
            }
        }
    }
}

private struct DisneyBrowseScreen: View {
    var body: some View {
        NavigationStack {
            List(DisneyDemoData.rows.flatMap { $0.items }, id: \.self) { title in
                HStack {
                    RoundedRectangle(cornerRadius: 4).fill(DisneyTokens.dpSurface1).frame(width: 80, height: 120)
                    VStack(alignment: .leading, spacing: 4) {
                        Text(title).font(.system(size: 17, weight: .semibold)).foregroundStyle(DisneyTokens.dpTextPrimary)
                        Text("Nouveauté · 2026").font(.system(size: 13)).foregroundStyle(DisneyTokens.dpTextSecondary)
                    }
                }
                .listRowBackground(DisneyTokens.dpCanvas)
            }
            .scrollContentBackground(.hidden)
            .background(DisneyTokens.dpCanvas.ignoresSafeArea())
            .navigationTitle("Parcourir")
        }
    }
}

private struct DisneyProfilePickerScreen: View {
    let profiles = DisneyDemoData.profiles
    var body: some View {
        ZStack {
            DisneyTokens.dpCanvas.ignoresSafeArea()
            VStack(spacing: 24) {
                Text("Qui regarde ?").font(.system(size: 32, weight: .bold)).foregroundStyle(DisneyTokens.dpTextPrimary)
                HStack(spacing: 20) {
                    ForEach(profiles) { p in
                        VStack(spacing: 8) {
                            RoundedRectangle(cornerRadius: 4).fill(p.color).frame(width: 84, height: 84)
                                .overlay(Text(p.emoji).font(.system(size: 36)))
                            Text(p.name).foregroundStyle(DisneyTokens.dpTextSecondary)
                        }
                    }
                    VStack(spacing: 8) {
                        RoundedRectangle(cornerRadius: 4).strokeBorder(DisneyTokens.dpTextSecondary, lineWidth: 1).frame(width: 84, height: 84)
                            .overlay(Image(systemName: "plus").font(.title).foregroundStyle(DisneyTokens.dpTextSecondary))
                        Text("Ajouter").foregroundStyle(DisneyTokens.dpTextSecondary)
                    }
                }
            }
        }
    }
}

private struct DisneyPosterRowData: Identifiable { let id = UUID(); let title: String; let items: [String] }
private struct DisneyProfile: Identifiable { let id = UUID(); let name: String; let emoji: String; let color: Color }

private enum DisneyDemoData {
    static let rows: [DisneyPosterRowData] = [
        .init(title: "Tendances actuelles", items: ["Midnight", "Horizon", "Nexus", "Pulse"]),
        .init(title: "Continuer à regarder", items: ["Saison 2 · Ép. 4", "Film · 42 min"]),
        .init(title: "Ajouts récents", items: ["Atlas", "Rivage", "Signal"]),
    ]
    static let profiles: [DisneyProfile] = [
        .init(name: "Alex", emoji: "🎬", color: DisneyTokens.dpLiveRed),
        .init(name: "Enfants", emoji: "🧒", color: .orange),
    ]
}

private struct DisneyGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(DisneyTokens.dpLiveRed.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}

