import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/finance/paypal/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomePayPalView: View {
    var body: some View {
        TabView {
            PayPalTabHost(title: "Activity", icon: "clock") { PayPalScreenFactory.screen(for: "Activity") }
                .tabItem { Label("Activity", systemImage: "clock") }
            PayPalTabHost(title: "Finances", icon: "chart.line.uptrend.xyaxis") { PayPalScreenFactory.screen(for: "Finances") }
                .tabItem { Label("Finances", systemImage: "chart.line.uptrend.xyaxis") }
        }
        .tint(PayPalTokens.payPalBlue)
    }
}

private struct PayPalTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum PayPalScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("activity"):
            PayPalHomeScreen()
        case let t where t.contains("card") || t.contains("invest") || t.contains("crypto") || t.contains("market") || t.contains("trade") || t.contains("financ"):
            PayPalCardsScreen()
        case let t where t.contains("recipient") || t.contains("plus"):
            PayPalMoreScreen()
        default:
            PayPalGenericListScreen(title: tab)
        }
    }
}
private enum PayPalTokens {
    static let payPalBlue = Color(red: 0.00, green: 0.188, blue: 0.529)  // #003087
    static let payPalSky = Color(red: 0.00, green: 0.439, blue: 0.729)  // #0070BA
    static let payPalCobalt = Color(red: 0.00, green: 0.110, blue: 0.392)  // #001C64
    static let payPalBlueDark = Color(red: 0.231, green: 0.510, blue: 0.965) // #3B82F6 (dark-mode shifted)
    static let ppCanvas = Color(red: 1.00, green: 1.00, blue: 1.00)    // #FFFFFF
    static let ppSurfaceGray = Color(red: 0.961, green: 0.969, blue: 0.980) // #F5F7FA
    static let ppSurfaceGray2 = Color(red: 0.933, green: 0.945, blue: 0.957) // #EEF1F4
    static let ppDivider = Color(red: 0.898, green: 0.910, blue: 0.929) // #E5E8ED
    static let ppTextPrimary = Color(red: 0.00, green: 0.078, blue: 0.208)  // #001435
    static let ppTextSecondary = Color(red: 0.173, green: 0.180, blue: 0.184) // #2C2E2F
    static let ppTextMuted = Color(red: 0.408, green: 0.443, blue: 0.451) // #687173
    static let ppTextTertiary = Color(red: 0.616, green: 0.639, blue: 0.651) // #9DA3A6
    static let ppSuccess = Color(red: 0.110, green: 0.545, blue: 0.263) // #1C8B43
    static let ppSuccessBg = Color(red: 0.894, green: 0.961, blue: 0.918) // #E4F5EA
    static let ppError = Color(red: 0.824, green: 0.00, blue: 0.129)  // #D20021
    static let ppErrorBg = Color(red: 0.988, green: 0.898, blue: 0.910) // #FCE5E8
    static let ppWarning = Color(red: 1.00, green: 0.722, blue: 0.110)  // #FFB81C
    static let ppWarningBg = Color(red: 1.00, green: 0.965, blue: 0.878)  // #FFF6E0
    static let ppIconSent = payPalBlue       // #003087
    static let ppIconReceived = ppSuccess        // #1C8B43
    static let ppIconCard = payPalSky        // #0070BA
    static let ppIconReward = ppWarning        // #FFB81C
    static let ppDarkCanvas = Color(red: 0.039, green: 0.055, blue: 0.102) // #0A0E1A
    static let ppDarkSurface1 = Color(red: 0.078, green: 0.102, blue: 0.165) // #141A2A
    static let ppDarkSurface2 = Color(red: 0.122, green: 0.153, blue: 0.251) // #1F2740
    static let ppDarkDivider = Color(red: 0.165, green: 0.192, blue: 0.259) // #2A3142
    static let ppDarkTextPri = white
    static let ppDarkTextSec = Color(red: 0.659, green: 0.682, blue: 0.769) // #A8AEC4
}

private enum PayPalFonts {
    static let ppBalanceHero = Font.system(size: 36, weight: .regular)
    static let ppSendAmountHero = Font.system(size: 56, weight: .regular)
    static let ppScreenTitle = Font.system(size: 24, weight: .regular)
    static let ppSectionHeader = Font.system(size: 18, weight: .regular)
    static let ppCardTitle = Font.system(size: 16, weight: .regular)
    static let ppActivityAmount = Font.system(size: 16, weight: .regular)
    static let ppBody = Font.system(size: 16, weight: .regular)
    static let ppBodySmall = Font.system(size: 14, weight: .regular)
    static let ppActivityTitle = Font.system(size: 15, weight: .regular)
    static let ppActivitySub = Font.system(size: 13, weight: .regular)
    static let ppMeta = Font.system(size: 13, weight: .regular)
    static let ppLink = Font.system(size: 15, weight: .regular)
    static let ppTab = Font.system(size: 11, weight: .regular)
    static let ppChip = Font.system(size: 13, weight: .regular)
    static let ppButton = Font.system(size: 17, weight: .regular)
    static let ppButtonSmall = Font.system(size: 15, weight: .regular)
}
private struct PayPalHomeScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 20) {
                    VStack(alignment: .leading, spacing: 8) {
                        Text("Solde total").font(.system(size: 14)).foregroundStyle(PayPalTokens.ppTextSecondary)
                        Text("2 847,50 €").font(.system(size: 36, weight: .bold)).foregroundStyle(PayPalTokens.ppTextPrimary)
                        HStack(spacing: 16) {
                            Label("Ajouter", systemImage: "plus")
                            Label("Envoyer", systemImage: "arrow.up.right")
                            Label("Échanger", systemImage: "arrow.left.arrow.right")
                        }.font(.system(size: 14, weight: .semibold)).foregroundStyle(PayPalTokens.payPalBlue).padding(.top, 4)
                    }.frame(maxWidth: .infinity, alignment: .leading)
                    RoundedRectangle(cornerRadius: 16).fill(LinearGradient(colors: [PayPalTokens.payPalBlue, PayPalTokens.payPalBlue.opacity(0.6)], startPoint: .topLeading, endPoint: .bottomTrailing))
                        .frame(height: 180).overlay(alignment: .bottomLeading) {
                            VStack(alignment: .leading) { Text("Carte Virtuelle").font(.system(size: 13)).foregroundStyle(.white.opacity(0.8)); Text("•••• 4829").font(.system(size: 22, weight: .bold)).foregroundStyle(.white) }.padding(20)
                        }
                    VStack(alignment: .leading, spacing: 12) {
                        Text("Transactions").font(.system(size: 18, weight: .bold))
                        ForEach(PayPalDemoData.transactions) { tx in PayPalTransactionRow(tx: tx) }
                    }
                }.padding()
            }.background(PayPalTokens.ppCanvas.ignoresSafeArea()).navigationTitle("PayPal")
        }
    }
}

private struct PayPalTransactionRow: View {
    let tx: PayPalTransaction
    var body: some View {
        HStack(spacing: 12) {
            Circle().fill(tx.color.opacity(0.2)).frame(width: 44, height: 44).overlay(Image(systemName: tx.icon).foregroundStyle(tx.color))
            VStack(alignment: .leading) { Text(tx.title).font(.system(size: 16, weight: .medium)); Text(tx.date).font(.system(size: 13)).foregroundStyle(.secondary) }
            Spacer()
            Text(tx.amount).font(.system(size: 16, weight: .semibold)).foregroundStyle(tx.amount.hasPrefix("-") ? .primary : .green)
        }
    }
}

private struct PayPalCardsScreen: View {
    var body: some View { NavigationStack { Text("Gérez vos cartes").padding().navigationTitle("Cartes") } }
}

private struct PayPalMoreScreen: View {
    var body: some View {
        NavigationStack { List { Section("Compte") { Label("Profil", systemImage: "person"); Label("Sécurité", systemImage: "lock") } }.navigationTitle("Plus") }
    }
}

private struct PayPalTransaction: Identifiable { let id = UUID(); let title: String; let date: String; let amount: String; let icon: String; let color: Color }
private enum PayPalDemoData {
    static let transactions: [PayPalTransaction] = [
        .init(title: "Monoprix", date: "Aujourd'hui", amount: "-24,80 €", icon: "cart.fill", color: .orange),
        .init(title: "Virement reçu", date: "Hier", amount: "+150,00 €", icon: "arrow.down.left", color: .green),
        .init(title: "Netflix", date: "28 juin", amount: "-13,49 €", icon: "play.tv.fill", color: .red),
    ]
}

private struct PayPalGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(PayPalTokens.payPalBlue.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}
