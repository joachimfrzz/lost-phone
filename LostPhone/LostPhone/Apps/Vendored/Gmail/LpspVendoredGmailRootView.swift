import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredGmail`).
struct LpspVendoredGmailRootView: View {
    var body: some View {
        VendoredGmailContentView()
    }
}
