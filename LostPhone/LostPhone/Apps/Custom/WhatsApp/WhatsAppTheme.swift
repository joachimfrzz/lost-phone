import SwiftUI

enum WhatsAppTheme {
    static let background = Color(hex: "#0B141A")
    static let listBackground = Color(hex: "#111B21")
    static let header = Color(hex: "#1F2C34")
    static let accent = Color(hex: "#00A884")
    static let sentBubble = Color(hex: "#005C4B")
    static let receivedBubble = Color(hex: "#1F2C34")
    static let secondaryText = Color(hex: "#8696A0")
    static let divider = Color(hex: "#222D34")
    static let unreadBadge = Color(hex: "#25D366")
}
