import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/social/linkedin/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeLinkedInView: View {
    var body: some View {
        TabView {
            LinkedInTabHost(title: "Home", icon: "house.fill") { LinkedInScreenFactory.screen(for: "Home") }
                .tabItem { Label("Home", systemImage: "house.fill") }
            LinkedInTabHost(title: "My Network", icon: "person.2.fill") { LinkedInScreenFactory.screen(for: "My Network") }
                .tabItem { Label("My Network", systemImage: "person.2.fill") }
            LinkedInTabHost(title: "Post", icon: "plus.app.fill") { LinkedInScreenFactory.screen(for: "Post") }
                .tabItem { Label("Post", systemImage: "plus.app.fill") }
            LinkedInTabHost(title: "Notifications", icon: "bell.fill") { LinkedInScreenFactory.screen(for: "Notifications") }
                .tabItem { Label("Notifications", systemImage: "bell.fill") }
            LinkedInTabHost(title: "Jobs", icon: "briefcase.fill") { LinkedInScreenFactory.screen(for: "Jobs") }
                .tabItem { Label("Jobs", systemImage: "briefcase.fill") }
        }
        .tint(LinkedInTokens.liBlue)
    }
}

private struct LinkedInTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum LinkedInScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("feed") || t.contains("notification"):
            LinkedInFeedScreen()
        case let t where t.contains("explor") || t.contains("search") || t.contains("commun"):
            LinkedInExploreScreen()
        case let t where t.contains("reel") || t.contains("short") || t.contains("create") || t.contains("post"):
            LinkedInReelsScreen()
        case let t where t.contains("profil") || t.contains("profile") || t.contains("network"):
            LinkedInProfileScreen()
        default:
            LinkedInGenericListScreen(title: tab)
        }
    }
}
private enum LinkedInTokens {
    static let liCanvas = Color(red: 0.953, green: 0.949, blue: 0.937)  // #F3F2EF
    static let liCardSurface = white                                    // #FFFFFF
    static let liElevated = Color(red: 0.976, green: 0.976, blue: 0.976)  // #F9F9F9
    static let liDivider = Color(red: 0.878, green: 0.875, blue: 0.863)  // #E0DFDC
    static let liDividerSubtle = Color(red: 0.929, green: 0.929, blue: 0.929)  // #EDEDED
    static let liTextPrimary = black.opacity(0.9)                      // #000000E6
    static let liTextSecondary = black.opacity(0.6)                      // #00000099
    static let liTextTertiary = black.opacity(0.4)                      // #00000066
    static let liBlue = Color(red: 0.039, green: 0.400, blue: 0.761)  // #0A66C2
    static let liBluePressed = Color(red: 0.000, green: 0.255, blue: 0.510)  // #004182
    static let liBlueSubtle = Color(red: 0.906, green: 0.953, blue: 1.000)  // #E7F3FF
    static let liOpenToWork = Color(red: 0.020, green: 0.463, blue: 0.259)  // #057642
    static let liPremiumGold = Color(red: 0.569, green: 0.349, blue: 0.027)  // #915907
    static let liPremiumGoldHi = Color(red: 0.765, green: 0.490, blue: 0.086)  // #C37D16
    static let liReactLike = Color(red: 0.039, green: 0.400, blue: 0.761)  // #0A66C2
    static let liReactCelebrate = Color(red: 0.961, green: 0.733, blue: 0.000)  // #F5BB00
    static let liReactSupport = Color(red: 0.698, green: 0.251, blue: 0.125)  // #B24020
    static let liReactLove = Color(red: 0.875, green: 0.439, blue: 0.302)  // #DF704D
    static let liReactInsightful = Color(red: 0.906, green: 0.639, blue: 0.243) // #E7A33E
    static let liReactFunny = Color(red: 0.000, green: 0.627, blue: 0.863)  // #00A0DC
    static let liDarkCanvas = Color(red: 0.106, green: 0.122, blue: 0.137)  // #1B1F23
    static let liDarkCard = Color(red: 0.114, green: 0.133, blue: 0.149)  // #1D2226
    static let liDarkBlue = Color(red: 0.439, green: 0.710, blue: 0.976)  // #70B5F9
}

private enum LinkedInFonts {
    static let liProfileName = Font.system(size: 24, weight: .bold)
    static let liScreenTitle = Font.system(size: 20, weight: .bold)
    static let liSectionHeader = Font.system(size: 16, weight: .bold)
    static let liPostAuthor = Font.system(size: 14, weight: .semibold)
    static let liHeadline = Font.system(size: 12, weight: .regular)
    static let liPostBody = Font.system(size: 14, weight: .regular)
    static let liMeta = Font.system(size: 12, weight: .regular)
    static let liButtonPrimary = Font.system(size: 16, weight: .semibold)
    static let liButtonSecondary = Font.system(size: 14, weight: .semibold)
    static let liActionBar = Font.system(size: 13, weight: .semibold)
    static let liTab = Font.system(size: 11, weight: .medium)
    static let liBadge = Font.system(size: 11, weight: .bold)
}
private struct LinkedInFeedScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 0) {
                    LinkedInStoriesBar()
                    ForEach(LinkedInDemoData.posts) { post in
                        LinkedInPostCard(post: post, accent: LinkedInTokens.liBlue, textSec: LinkedInTokens.liTextSecondary)
                        Divider().background(LinkedInTokens.liTextSecondary.opacity(0.3))
                    }
                }
            }
            .background(LinkedInTokens.liCanvas.ignoresSafeArea())
            .navigationTitle("LinkedIn")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .topBarLeading) { Image(systemName: "camera").foregroundStyle(LinkedInTokens.liBlue) }
                ToolbarItem(placement: .topBarTrailing) {
                    HStack(spacing: 16) {
                        Image(systemName: "heart")
                        Image(systemName: "paperplane")
                    }
                }
            }
        }
    }
}

private struct LinkedInStoriesBar: View {
    private let stories = LinkedInDemoData.stories
    var body: some View {
        ScrollView(.horizontal, showsIndicators: false) {
            HStack(spacing: 14) {
                ForEach(stories) { story in
                    VStack(spacing: 4) {
                        ZStack {
                            Circle().strokeBorder(
                                AngularGradient(colors: [.orange, .pink, .purple, .blue, .orange], center: .center),
                                lineWidth: story.unread ? 2.5 : 1
                            ).frame(width: 68, height: 68)
                            Circle().fill(story.color.gradient).frame(width: 60, height: 60)
                                .overlay(Text(story.initials).font(.system(size: 20, weight: .semibold)).foregroundStyle(.white))
                        }
                        Text(story.name).font(.system(size: 11)).lineLimit(1).frame(width: 72)
                    }
                }
            }
            .padding(.horizontal, 12).padding(.vertical, 10)
        }
    }
}

private struct LinkedInPostCard: View {
    let post: LinkedInPost
    let accent: Color
    let textSec: Color
    @State private var liked = false
    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            HStack {
                Circle().fill(post.avatarColor.gradient).frame(width: 32, height: 32)
                    .overlay(Text(post.initials).font(.system(size: 12, weight: .bold)).foregroundStyle(.white))
                Text(post.username).font(LinkedInFonts.liScreenTitle)
                Spacer()
                Image(systemName: "ellipsis")
            }
            .padding(.horizontal, 12)
            RoundedRectangle(cornerRadius: 0).fill(post.avatarColor.opacity(0.25)).frame(height: 320)
                .overlay { Image(systemName: "photo").font(.system(size: 48)).foregroundStyle(textSec) }
            HStack(spacing: 16) {
                Button { liked.toggle() } label: { Image(systemName: liked ? "heart.fill" : "heart").foregroundStyle(liked ? .red : .primary) }
                Image(systemName: "bubble.right")
                Image(systemName: "paperplane")
                Spacer()
                Image(systemName: "bookmark")
            }
            .font(.system(size: 22)).padding(.horizontal, 12)
            Text("\(post.likes + (liked ? 1 : 0)) J'aime").font(.system(size: 14, weight: .semibold)).padding(.horizontal, 12)
            Text(post.caption).font(.system(size: 14)).padding(.horizontal, 12).padding(.bottom, 12)
        }
    }
}

private struct LinkedInExploreScreen: View {
    let cols = [GridItem(.flexible()), GridItem(.flexible()), GridItem(.flexible())]
    var body: some View {
        NavigationStack {
            ScrollView {
                LazyVGrid(columns: cols, spacing: 2) {
                    ForEach(0..<18, id: \.self) { i in
                        RoundedRectangle(cornerRadius: 2).fill(Color(hue: Double(i) / 18, saturation: 0.3, brightness: 0.85))
                            .aspectRatio(1, contentMode: .fit)
                    }
                }
            }
            .navigationTitle("Explorer")
        }
    }
}

private struct LinkedInReelsScreen: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            VStack {
                Spacer()
                Image(systemName: "play.rectangle.fill").font(.system(size: 64)).foregroundStyle(.white.opacity(0.8))
                Text("Reels").font(.title2.bold()).foregroundStyle(.white)
                Text("Faites défiler pour découvrir").foregroundStyle(.white.opacity(0.7))
                Spacer()
            }
        }
    }
}

private struct LinkedInProfileScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 16) {
                    HStack(spacing: 24) {
                        Circle().fill(LinkedInTokens.liBlue.gradient).frame(width: 86, height: 86)
                            .overlay(Text("LP").font(.title.bold()).foregroundStyle(.white))
                        HStack(spacing: 20) {
                            LinkedInStatBlock(value: "128", label: "Publications")
                            LinkedInStatBlock(value: "1,2k", label: "Abonnés")
                            LinkedInStatBlock(value: "342", label: "Abonnements")
                        }
                    }
                    .padding(.horizontal)
                    Text("Joueur Lost Phone").font(.system(size: 14, weight: .semibold))
                    Text("Paris · Photographie · Voyage").font(.system(size: 14)).foregroundStyle(.secondary)
                    HStack {
                        Button {} label: { Text("Modifier le profil").font(.system(size: 14, weight: .semibold)).frame(maxWidth: .infinity).padding(.vertical, 8).background(RoundedRectangle(cornerRadius: 8).fill(LinkedInTokens.liTextSecondary.opacity(0.15))) }
                        Button {} label: { Text("Partager").font(.system(size: 14, weight: .semibold)).frame(maxWidth: .infinity).padding(.vertical, 8).background(RoundedRectangle(cornerRadius: 8).fill(LinkedInTokens.liTextSecondary.opacity(0.15))) }
                    }
                    .padding(.horizontal)
                }
                .padding(.top)
            }
            .navigationTitle("Profil")
        }
    }
}

private struct LinkedInStatBlock: View {
    let value: String; let label: String
    var body: some View {
        VStack { Text(value).font(.system(size: 16, weight: .bold)); Text(label).font(.system(size: 12)) }
    }
}

private struct LinkedInStory: Identifiable { let id = UUID(); let name: String; let initials: String; let color: Color; let unread: Bool }
private struct LinkedInPost: Identifiable { let id = UUID(); let username: String; let initials: String; let avatarColor: Color; let caption: String; let likes: Int }

private enum LinkedInDemoData {
    static let stories: [LinkedInStory] = [
        .init(name: "Votre story", initials: "+", color: .gray, unread: false),
        .init(name: "Camille", initials: "C", color: .pink, unread: true),
        .init(name: "Lucas", initials: "L", color: .blue, unread: true),
        .init(name: "Inès", initials: "I", color: .purple, unread: false),
    ]
    static let posts: [LinkedInPost] = [
        .init(username: "camille_p", initials: "C", avatarColor: .pink, caption: "Coucher de soleil sur la Seine 🌅 #paris", likes: 842),
        .init(username: "lucas.mrt", initials: "L", avatarColor: .blue, caption: "Nouveau café du quartier — à tester !", likes: 156),
    ]
}

private struct LinkedInGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(LinkedInTokens.liBlue.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}

