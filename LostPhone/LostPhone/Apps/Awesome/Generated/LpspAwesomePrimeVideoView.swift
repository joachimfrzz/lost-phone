import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/video/prime-video/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomePrimeVideoView: View {
    var body: some View {
        TabView {
            PrimeVideoTabHost(title: "Downloads", icon: "arrow.down.circle.fill") { PrimeVideoScreenFactory.screen(for: "Downloads") }
                .tabItem { Label("Downloads", systemImage: "arrow.down.circle.fill") }
        }
        .tint(PrimeVideoTokens.primeLiveRed)
    }
}

private struct PrimeVideoTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum PrimeVideoScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("watch"):
            PrimeVideoHomeScreen()
        case let t where t.contains("search") || t.contains("recherch") || t.contains("new") || t.contains("hot") || t.contains("short"):
            PrimeVideoBrowseScreen()
        case let t where t.contains("profil") || t.contains("profile") || t.contains("netflix") || t.contains("watchlist") || t.contains("store") || t.contains("download") || t.contains("notif"):
            PrimeVideoProfilePickerScreen()
        default:
            PrimeVideoGenericListScreen(title: tab)
        }
    }
}
private enum PrimeVideoTokens {
    static let primeCanvas = Color(red: 0.059, green: 0.090, blue: 0.118) // #0F171E
    static let primeDeepBlack = Color.black                                  // #000000
    static let primeSurface1 = Color(red: 0.102, green: 0.141, blue: 0.184) // #1A242F
    static let primeSurface2 = Color(red: 0.137, green: 0.184, blue: 0.243) // #232F3E
    static let primeSurface3 = Color(red: 0.180, green: 0.231, blue: 0.278) // #2E3B47
    static let primeDivider = Color(red: 0.180, green: 0.231, blue: 0.278) // #2E3B47
    static let primeTextPrimary = Color.white                                // #FFFFFF
    static let primeTextSecondary = Color(red: 0.667, green: 0.718, blue: 0.769) // #AAB7C4
    static let primeTextTertiary = Color(red: 0.431, green: 0.482, blue: 0.537) // #6E7B89
    static let primeBlue = Color(red: 0.000, green: 0.659, blue: 0.882) // #00A8E1
    static let primeBluePressed = Color(red: 0.000, green: 0.549, blue: 0.741) // #008CBD
    static let primeImdbYellow = Color(red: 0.961, green: 0.773, blue: 0.094) // #F5C518
    static let primeLiveRed = Color(red: 0.898, green: 0.035, blue: 0.078) // #E50914
}

private enum PrimeVideoFonts {
    static let primeDetailsTitle = Font.system(size: 30, weight: .regular)
    static let primeTitleLarge = Font.system(size: 28, weight: .regular)
    static let primeRowHeader = Font.system(size: 22, weight: .regular)
    static let primeSectionHeader = Font.system(size: 20, weight: .regular)
    static let primeEpisodeTitle = Font.system(size: 16, weight: .regular)
    static let primeTileTitle = Font.system(size: 15, weight: .regular)
    static let primeBody = Font.system(size: 15, weight: .regular)
    static let primeMeta = Font.system(size: 13, weight: .regular)
    static let primeTileSubtitle = Font.system(size: 12, weight: .regular)
    static let primeLabelUpper = Font.system(size: 11, weight: .regular)
    static let primeButton = Font.system(size: 16, weight: .regular)
    static let primeButtonSecondary = Font.system(size: 15, weight: .regular)
    static let primeTab = Font.system(size: 10, weight: .regular)
    static let primeBadge = Font.system(size: 11, weight: .regular)
}
private struct PrimeVideoHomeScreen: View {
    var body: some View {
        NavigationStack {
            ZStack(alignment: .bottom) {
                ScrollView {
                    VStack(alignment: .leading, spacing: 20) {
                        PrimeVideoHeroBillboard()
                        ForEach(PrimeVideoDemoData.rows) { row in
                            PrimeVideoPosterRow(title: row.title, items: row.items, accent: PrimeVideoTokens.primeLiveRed)
                        }
                    }
                    .padding(.bottom, 24)
                }
                .background(PrimeVideoTokens.primeCanvas.ignoresSafeArea())
            }
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .principal) { Text("Prime Video").font(.system(size: 20, weight: .bold)).foregroundStyle(PrimeVideoTokens.primeTextPrimary) }
                ToolbarItem(placement: .topBarTrailing) { Image(systemName: "person.crop.circle").foregroundStyle(PrimeVideoTokens.primeTextPrimary) }
            }
            .toolbarBackground(PrimeVideoTokens.primeCanvas, for: .navigationBar)
        }
    }
}

private struct PrimeVideoHeroBillboard: View {
    var body: some View {
        ZStack(alignment: .bottomLeading) {
            RoundedRectangle(cornerRadius: 4).fill(
                LinearGradient(colors: [PrimeVideoTokens.primeSurface1, .black.opacity(0.3)], startPoint: .top, endPoint: .bottom)
            ).aspectRatio(16/9, contentMode: .fit)
            VStack(alignment: .leading, spacing: 8) {
                Text("SÉRIE TENDANCE").font(.system(size: 11, weight: .bold)).foregroundStyle(PrimeVideoTokens.primeTextSecondary)
                Text("L'Énigme de Belleville").font(.system(size: 28, weight: .bold)).foregroundStyle(.white)
                HStack(spacing: 12) {
                    Label("Lecture", systemImage: "play.fill").font(.system(size: 15, weight: .bold))
                        .frame(maxWidth: .infinity).padding(.vertical, 10)
                        .background(RoundedRectangle(cornerRadius: 4).fill(PrimeVideoTokens.primeLiveRed))
                    Label("Plus d'infos", systemImage: "info.circle").font(.system(size: 15, weight: .semibold))
                        .frame(maxWidth: .infinity).padding(.vertical, 10)
                        .background(RoundedRectangle(cornerRadius: 4).fill(PrimeVideoTokens.primeSurface1))
                }
            }
            .padding(16)
        }
        .padding(.horizontal, 12)
    }
}

private struct PrimeVideoPosterRow: View {
    let title: String; let items: [String]; let accent: Color
    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            Text(title).font(.system(size: 17, weight: .bold)).foregroundStyle(.white).padding(.horizontal, 12)
            ScrollView(.horizontal, showsIndicators: false) {
                HStack(spacing: 8) {
                    ForEach(items, id: \.self) { item in
                        RoundedRectangle(cornerRadius: 4).fill(accent.opacity(0.35)).frame(width: 110, height: 160)
                            .overlay(alignment: .bottomLeading) {
                                Text(item).font(.system(size: 11, weight: .semibold)).foregroundStyle(.white).padding(6)
                            }
                    }
                }
                .padding(.horizontal, 12)
            }
        }
    }
}

private struct PrimeVideoBrowseScreen: View {
    var body: some View {
        NavigationStack {
            List(PrimeVideoDemoData.rows.flatMap { $0.items }, id: \.self) { title in
                HStack {
                    RoundedRectangle(cornerRadius: 4).fill(PrimeVideoTokens.primeSurface1).frame(width: 80, height: 120)
                    VStack(alignment: .leading, spacing: 4) {
                        Text(title).font(.system(size: 17, weight: .semibold)).foregroundStyle(PrimeVideoTokens.primeTextPrimary)
                        Text("Nouveauté · 2026").font(.system(size: 13)).foregroundStyle(PrimeVideoTokens.primeTextSecondary)
                    }
                }
                .listRowBackground(PrimeVideoTokens.primeCanvas)
            }
            .scrollContentBackground(.hidden)
            .background(PrimeVideoTokens.primeCanvas.ignoresSafeArea())
            .navigationTitle("Parcourir")
        }
    }
}

private struct PrimeVideoProfilePickerScreen: View {
    let profiles = PrimeVideoDemoData.profiles
    var body: some View {
        ZStack {
            PrimeVideoTokens.primeCanvas.ignoresSafeArea()
            VStack(spacing: 24) {
                Text("Qui regarde ?").font(.system(size: 32, weight: .bold)).foregroundStyle(PrimeVideoTokens.primeTextPrimary)
                HStack(spacing: 20) {
                    ForEach(profiles) { p in
                        VStack(spacing: 8) {
                            RoundedRectangle(cornerRadius: 4).fill(p.color).frame(width: 84, height: 84)
                                .overlay(Text(p.emoji).font(.system(size: 36)))
                            Text(p.name).foregroundStyle(PrimeVideoTokens.primeTextSecondary)
                        }
                    }
                    VStack(spacing: 8) {
                        RoundedRectangle(cornerRadius: 4).strokeBorder(PrimeVideoTokens.primeTextSecondary, lineWidth: 1).frame(width: 84, height: 84)
                            .overlay(Image(systemName: "plus").font(.title).foregroundStyle(PrimeVideoTokens.primeTextSecondary))
                        Text("Ajouter").foregroundStyle(PrimeVideoTokens.primeTextSecondary)
                    }
                }
            }
        }
    }
}

private struct PrimeVideoPosterRowData: Identifiable { let id = UUID(); let title: String; let items: [String] }
private struct PrimeVideoProfile: Identifiable { let id = UUID(); let name: String; let emoji: String; let color: Color }

private enum PrimeVideoDemoData {
    static let rows: [PrimeVideoPosterRowData] = [
        .init(title: "Tendances actuelles", items: ["Midnight", "Horizon", "Nexus", "Pulse"]),
        .init(title: "Continuer à regarder", items: ["Saison 2 · Ép. 4", "Film · 42 min"]),
        .init(title: "Ajouts récents", items: ["Atlas", "Rivage", "Signal"]),
    ]
    static let profiles: [PrimeVideoProfile] = [
        .init(name: "Alex", emoji: "🎬", color: PrimeVideoTokens.primeLiveRed),
        .init(name: "Enfants", emoji: "🧒", color: .orange),
    ]
}

private struct PrimeVideoGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(PrimeVideoTokens.primeLiveRed.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}

