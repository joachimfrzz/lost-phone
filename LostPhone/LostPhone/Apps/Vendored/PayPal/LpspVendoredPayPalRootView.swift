import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredPayPal`).
struct LpspVendoredPayPalRootView: View {
    var body: some View {
        VendoredPayPalContentView()
    }
}
