import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/finance/binance/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeBinanceView: View {
    var body: some View {
        TabView {
            BinanceTabHost(title: "Home", icon: "house.fill") { BinanceScreenFactory.screen(for: "Home") }
                .tabItem { Label("Home", systemImage: "house.fill") }
            BinanceTabHost(title: "Markets", icon: "chart.bar.fill") { BinanceScreenFactory.screen(for: "Markets") }
                .tabItem { Label("Markets", systemImage: "chart.bar.fill") }
            BinanceTabHost(title: "Trade", icon: "arrow.left.arrow.right") { BinanceScreenFactory.screen(for: "Trade") }
                .tabItem { Label("Trade", systemImage: "arrow.left.arrow.right") }
            BinanceTabHost(title: "Futures", icon: "chart.xyaxis.line") { BinanceScreenFactory.screen(for: "Futures") }
                .tabItem { Label("Futures", systemImage: "chart.xyaxis.line") }
            BinanceTabHost(title: "Wallets", icon: "creditcard.fill") { BinanceScreenFactory.screen(for: "Wallets") }
                .tabItem { Label("Wallets", systemImage: "creditcard.fill") }
        }
        .tint(BinanceTokens.bnTextPrimary)
    }
}

private struct BinanceTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum BinanceScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("activity"):
            BinanceHomeScreen()
        case let t where t.contains("card") || t.contains("invest") || t.contains("crypto") || t.contains("market") || t.contains("trade") || t.contains("financ"):
            BinanceCardsScreen()
        case let t where t.contains("recipient") || t.contains("plus"):
            BinanceMoreScreen()
        default:
            BinanceGenericListScreen(title: tab)
        }
    }
}
private enum BinanceTokens {
    static let bnCanvas = Color(red: 0.043, green: 0.055, blue: 0.067) // #0B0E11
    static let bnSurface1 = Color(red: 0.094, green: 0.102, blue: 0.125) // #181A20
    static let bnSurface2 = Color(red: 0.118, green: 0.125, blue: 0.149) // #1E2026
    static let bnSurface3 = Color(red: 0.169, green: 0.192, blue: 0.224) // #2B3139
    static let bnDivider = Color(red: 0.169, green: 0.192, blue: 0.224) // #2B3139
    static let bnCanvasLight = white                                  // #FFFFFF
    static let bnSurface1Light = Color(red: 0.961, green: 0.961, blue: 0.961) // #F5F5F5
    static let bnDividerLight = Color(red: 0.918, green: 0.925, blue: 0.937) // #EAECEF
    static let bnYellow = Color(red: 0.941, green: 0.725, blue: 0.043) // #F0B90B
    static let bnYellowPressed = Color(red: 0.788, green: 0.580, blue: 0.0)  // #C99400
    static let bnUp = Color(red: 0.055, green: 0.796, blue: 0.506) // #0ECB81
    static let bnDown = Color(red: 0.965, green: 0.275, blue: 0.365) // #F6465D
    static let bnUpPressed = Color(red: 0.043, green: 0.647, blue: 0.447) // #0BA572
    static let bnDownPressed = Color(red: 0.851, green: 0.220, blue: 0.286) // #D93849
    static let bnTextPrimary = Color(red: 0.918, green: 0.925, blue: 0.937) // #EAECEF
    static let bnTextSecondary = Color(red: 0.518, green: 0.557, blue: 0.612) // #848E9C
    static let bnTextTertiary = Color(red: 0.369, green: 0.400, blue: 0.451) // #5E6673
    static let bnTextPrimaryLt = Color(red: 0.118, green: 0.137, blue: 0.161) // #1E2329
    static let bnInfo = Color(red: 0.200, green: 0.459, blue: 0.733) // #3375BB
    static let bnYellowTint = bnYellow.opacity(0.12)
    static let bnAskFill = bnDown.opacity(0.14)
    static let bnBidFill = bnUp.opacity(0.14)
}

private enum BinanceFonts {
    static let bnScreenTitle = Font.system(size: 32, weight: .regular)
    static let bnSection = Font.system(size: 22, weight: .regular)
    static let bnRowTitle = Font.system(size: 18, weight: .regular)
    static let bnBody = Font.system(size: 16, weight: .regular)
    static let bnListLabel = Font.system(size: 14, weight: .regular)
    static let bnMeta = Font.system(size: 14, weight: .regular)
    static let bnPill = Font.system(size: 13, weight: .regular)
    static let bnCaption = Font.system(size: 12, weight: .regular)
    static let bnTab = Font.system(size: 10, weight: .regular)
    static let bnButton = Font.system(size: 15, weight: .regular)
    static let bnBalance = Font.system(size: 28, weight: .regular)
    static let bnPrice = Font.system(size: 15, weight: .regular)
    static let bnNumber = Font.system(size: 14, weight: .regular)
    static let bnMonoCaption = Font.system(size: 12, weight: .regular)
    static let bnMonoSmall = Font.system(size: 10, weight: .regular)
}
private struct BinanceHomeScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 20) {
                    VStack(alignment: .leading, spacing: 8) {
                        Text("Solde total").font(.system(size: 14)).foregroundStyle(BinanceTokens.bnTextSecondary)
                        Text("2 847,50 €").font(.system(size: 36, weight: .bold)).foregroundStyle(BinanceTokens.bnTextPrimary)
                        HStack(spacing: 16) {
                            Label("Ajouter", systemImage: "plus")
                            Label("Envoyer", systemImage: "arrow.up.right")
                            Label("Échanger", systemImage: "arrow.left.arrow.right")
                        }.font(.system(size: 14, weight: .semibold)).foregroundStyle(BinanceTokens.bnTextPrimary).padding(.top, 4)
                    }.frame(maxWidth: .infinity, alignment: .leading)
                    RoundedRectangle(cornerRadius: 16).fill(LinearGradient(colors: [BinanceTokens.bnTextPrimary, BinanceTokens.bnTextPrimary.opacity(0.6)], startPoint: .topLeading, endPoint: .bottomTrailing))
                        .frame(height: 180).overlay(alignment: .bottomLeading) {
                            VStack(alignment: .leading) { Text("Carte Virtuelle").font(.system(size: 13)).foregroundStyle(.white.opacity(0.8)); Text("•••• 4829").font(.system(size: 22, weight: .bold)).foregroundStyle(.white) }.padding(20)
                        }
                    VStack(alignment: .leading, spacing: 12) {
                        Text("Transactions").font(.system(size: 18, weight: .bold))
                        ForEach(BinanceDemoData.transactions) { tx in BinanceTransactionRow(tx: tx) }
                    }
                }.padding()
            }.background(BinanceTokens.bnCanvas.ignoresSafeArea()).navigationTitle("Binance")
        }
    }
}

private struct BinanceTransactionRow: View {
    let tx: BinanceTransaction
    var body: some View {
        HStack(spacing: 12) {
            Circle().fill(tx.color.opacity(0.2)).frame(width: 44, height: 44).overlay(Image(systemName: tx.icon).foregroundStyle(tx.color))
            VStack(alignment: .leading) { Text(tx.title).font(.system(size: 16, weight: .medium)); Text(tx.date).font(.system(size: 13)).foregroundStyle(.secondary) }
            Spacer()
            Text(tx.amount).font(.system(size: 16, weight: .semibold)).foregroundStyle(tx.amount.hasPrefix("-") ? .primary : .green)
        }
    }
}

private struct BinanceCardsScreen: View {
    var body: some View { NavigationStack { Text("Gérez vos cartes").padding().navigationTitle("Cartes") } }
}

private struct BinanceMoreScreen: View {
    var body: some View {
        NavigationStack { List { Section("Compte") { Label("Profil", systemImage: "person"); Label("Sécurité", systemImage: "lock") } }.navigationTitle("Plus") }
    }
}

private struct BinanceTransaction: Identifiable { let id = UUID(); let title: String; let date: String; let amount: String; let icon: String; let color: Color }
private enum BinanceDemoData {
    static let transactions: [BinanceTransaction] = [
        .init(title: "Monoprix", date: "Aujourd'hui", amount: "-24,80 €", icon: "cart.fill", color: .orange),
        .init(title: "Virement reçu", date: "Hier", amount: "+150,00 €", icon: "arrow.down.left", color: .green),
        .init(title: "Netflix", date: "28 juin", amount: "-13,49 €", icon: "play.tv.fill", color: .red),
    ]
}

private struct BinanceGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(BinanceTokens.bnTextPrimary.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}
