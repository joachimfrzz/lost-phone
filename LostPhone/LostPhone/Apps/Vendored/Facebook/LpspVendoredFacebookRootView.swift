import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredFacebook`).
struct LpspVendoredFacebookRootView: View {
    var body: some View {
        VendoredFacebookContentView()
    }
}
