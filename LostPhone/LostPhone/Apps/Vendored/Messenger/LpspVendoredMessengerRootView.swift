import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredMessenger`).
struct LpspVendoredMessengerRootView: View {
    var body: some View {
        VendoredMessengerContentView()
    }
}
