import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/music/apple-music/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeAppleMusicView: View {
    var body: some View {
        TabView {
            AppleMusicTabHost(title: "Home", icon: "house.fill") { AppleMusicScreenFactory.screen(for: "Home") }
                .tabItem { Label("Home", systemImage: "house.fill") }
            AppleMusicTabHost(title: "New", icon: "music.note.list") { AppleMusicScreenFactory.screen(for: "New") }
                .tabItem { Label("New", systemImage: "music.note.list") }
            AppleMusicTabHost(title: "Radio", icon: "dot.radiowaves.left.and.right") { AppleMusicScreenFactory.screen(for: "Radio") }
                .tabItem { Label("Radio", systemImage: "dot.radiowaves.left.and.right") }
            AppleMusicTabHost(title: "Library", icon: "square.stack.fill") { AppleMusicScreenFactory.screen(for: "Library") }
                .tabItem { Label("Library", systemImage: "square.stack.fill") }
            AppleMusicTabHost(title: "Search", icon: "magnifyingglass") { AppleMusicScreenFactory.screen(for: "Search") }
                .tabItem { Label("Search", systemImage: "magnifyingglass") }
        }
        .tint(AppleMusicTokens.amSystemGreen)
    }
}

private struct AppleMusicTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum AppleMusicScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("accueil") || t.contains("home") || t.contains("radio") || t.contains("new") || t.contains("sample"):
            AppleMusicHomeScreen()
        case let t where t.contains("search") || t.contains("recherch") || t.contains("discover"):
            AppleMusicSearchScreen()
        case let t where t.contains("library") || t.contains("biblioth") || t.contains("premium") || t.contains("profil"):
            AppleMusicLibraryScreen()
        default:
            AppleMusicGenericListScreen(title: tab)
        }
    }
}
private enum AppleMusicTokens {
    static let amRed = Color(red: 0.980, green: 0.176, blue: 0.282) // #FA2D48
    static let amCoral = Color(red: 0.988, green: 0.235, blue: 0.267) // #FC3C44
    static let amRedPressed = Color(red: 0.831, green: 0.129, blue: 0.231) // #D4213B
    static let amCanvasLight = Color.white                                  // #FFFFFF
    static let amCanvasDark = Color.black                                  // #000000
    static let amSurface1Light = Color(red: 0.949, green: 0.949, blue: 0.969) // #F2F2F7
    static let amSurface1Dark = Color(red: 0.110, green: 0.110, blue: 0.118) // #1C1C1E
    static let amSurface2Dark = Color(red: 0.173, green: 0.173, blue: 0.180) // #2C2C2E
    static let amSurface3Dark = Color(red: 0.227, green: 0.227, blue: 0.235) // #3A3A3C
    static let amDividerLight = Color(red: 0.776, green: 0.776, blue: 0.784) // #C6C6C8
    static let amDividerDark = Color(red: 0.220, green: 0.220, blue: 0.227) // #38383A
    static let amAtmosGold = Color(red: 0.831, green: 0.659, blue: 0.341) // #D4A857
    static let amLosslessSilver = Color(red: 0.557, green: 0.557, blue: 0.576) // #8E8E93
    static let amSystemBlue = Color(red: 0.0,   green: 0.478, blue: 1.0)   // #007AFF
    static let amSystemGreen = Color(red: 0.204, green: 0.780, blue: 0.349) // #34C759
    static let amSystemRed = Color(red: 1.0,   green: 0.231, blue: 0.188) // #FF3B30
}

private enum AppleMusicFonts {
    static let amLargeTitle = Font.system(size: 34, weight: .bold)
    static let amTitle1 = Font.system(size: 28, weight: .bold)
    static let amHeroListen = Font.system(size: 24, weight: .bold)
    static let amTitle2 = Font.system(size: 22, weight: .bold)
    static let amTitle3 = Font.system(size: 20, weight: .semibold)
    static let amNowPlaying = Font.system(size: 18, weight: .semibold)
    static let amHeadline = Font.system(size: 17, weight: .regular)
    static let amBody = Font.system(size: 17, weight: .regular)
    static let amCallout = Font.system(size: 17, weight: .regular)
    static let amSubheadline = Font.system(size: 17, weight: .regular)
    static let amFootnote = Font.system(size: 17, weight: .regular)
    static let amCaption1 = Font.system(size: 17, weight: .regular)
    static let amCaption2 = Font.system(size: 17, weight: .regular)
    static let amButton = Font.system(size: 17, weight: .semibold)
    static let amTabLabel = Font.system(size: 10, weight: .medium)
    static let amLyricsCurrent = Font.system(size: 28, weight: .bold)
}
private struct AppleMusicHomeScreen: View {
    var body: some View {
        ZStack(alignment: .bottom) {
            ScrollView {
                VStack(alignment: .leading, spacing: 20) {
                    Text("Bonsoir").font(.system(size: 28, weight: .bold)).foregroundStyle(AppleMusicTokens.amRed).padding(.horizontal)
                    LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())], spacing: 12) {
                        ForEach(AppleMusicDemoData.recent) { item in
                            AppleMusicRecentTile(item: item, surface: AppleMusicTokens.amSurface1Light)
                        }
                    }
                    .padding(.horizontal)
                    Text("Récemment joué").font(.system(size: 22, weight: .bold)).foregroundStyle(AppleMusicTokens.amRed).padding(.horizontal)
                    ScrollView(.horizontal, showsIndicators: false) {
                        HStack(spacing: 12) {
                            ForEach(AppleMusicDemoData.playlists) { pl in
                                VStack(alignment: .leading) {
                                    RoundedRectangle(cornerRadius: 6).fill(pl.color).frame(width: 140, height: 140)
                                    Text(pl.title).font(.system(size: 14, weight: .semibold)).foregroundStyle(AppleMusicTokens.amRed).lineLimit(1)
                                }
                            }
                        }
                        .padding(.horizontal)
                    }
                }
                .padding(.vertical).padding(.bottom, 80)
            }
            .background(AppleMusicTokens.amCanvasLight.ignoresSafeArea())
            AppleMusicMiniPlayer(accent: AppleMusicTokens.amSystemGreen, surface: AppleMusicTokens.amSurface1Light, textPri: AppleMusicTokens.amRed)
        }
    }
}

private struct AppleMusicRecentTile: View {
    let item: AppleMusicRecentItem; let surface: Color
    var body: some View {
        HStack(spacing: 10) {
            RoundedRectangle(cornerRadius: 4).fill(item.color).frame(width: 52, height: 52)
            Text(item.title).font(.system(size: 14, weight: .semibold)).foregroundStyle(.white).lineLimit(2)
            Spacer(minLength: 0)
        }
        .padding(8).background(RoundedRectangle(cornerRadius: 6).fill(surface))
    }
}

private struct AppleMusicMiniPlayer: View {
    let accent: Color; let surface: Color; let textPri: Color
    var body: some View {
        HStack(spacing: 12) {
            RoundedRectangle(cornerRadius: 4).fill(accent.opacity(0.5)).frame(width: 44, height: 44)
            VStack(alignment: .leading, spacing: 2) {
                Text("La Vie en Rose").font(.system(size: 14, weight: .semibold)).foregroundStyle(textPri)
                Text("Édith Piaf").font(.system(size: 12)).foregroundStyle(.secondary)
            }
            Spacer()
            Image(systemName: "play.fill").font(.title3)
            Image(systemName: "forward.end.fill").font(.title3)
        }
        .padding(.horizontal, 12).padding(.vertical, 10)
        .background(.ultraThinMaterial)
        .overlay(alignment: .top) { Divider() }
    }
}

private struct AppleMusicSearchScreen: View {
    var body: some View {
        NavigationStack {
            VStack(spacing: 16) {
                HStack {
                    Image(systemName: "magnifyingglass").foregroundStyle(.secondary)
                    Text("Artistes, titres ou podcasts").foregroundStyle(.secondary)
                    Spacer()
                }
                .padding().background(RoundedRectangle(cornerRadius: 8).fill(AppleMusicTokens.amSurface1Light))
                .padding()
                LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())], spacing: 12) {
                    ForEach(AppleMusicDemoData.genres, id: \.self) { g in
                        RoundedRectangle(cornerRadius: 8).fill(Color(hue: Double(abs(g.hashValue) % 360) / 360, saturation: 0.5, brightness: 0.7))
                            .frame(height: 100).overlay(alignment: .bottomLeading) { Text(g).font(.headline).padding(8) }
                    }
                }
                .padding(.horizontal)
                Spacer()
            }
            .background(AppleMusicTokens.amCanvasLight.ignoresSafeArea())
            .navigationTitle("Rechercher")
        }
    }
}

private struct AppleMusicLibraryScreen: View {
    var body: some View {
        NavigationStack {
            List(AppleMusicDemoData.playlists) { pl in
                HStack(spacing: 12) {
                    RoundedRectangle(cornerRadius: 4).fill(pl.color).frame(width: 48, height: 48)
                    VStack(alignment: .leading) {
                        Text(pl.title).font(.system(size: 16, weight: .semibold))
                        Text("Playlist · 42 titres").font(.system(size: 13)).foregroundStyle(.secondary)
                    }
                }
                .listRowBackground(AppleMusicTokens.amCanvasLight)
            }
            .scrollContentBackground(.hidden)
            .background(AppleMusicTokens.amCanvasLight.ignoresSafeArea())
            .navigationTitle("Bibliothèque")
        }
    }
}

private struct AppleMusicRecentItem: Identifiable { let id = UUID(); let title: String; let color: Color }
private struct AppleMusicPlaylist: Identifiable { let id = UUID(); let title: String; let color: Color }

private enum AppleMusicDemoData {
    static let recent: [AppleMusicRecentItem] = [
        .init(title: "Mix du jour", color: .purple),
        .init(title: "Découvertes", color: .blue),
        .init(title: "Podcasts FR", color: .orange),
        .init(title: "Chill", color: .teal),
    ]
    static let playlists: [AppleMusicPlaylist] = [
        .init(title: "Indie Française", color: .pink),
        .init(title: "Running Paris", color: .green),
        .init(title: "Années 80", color: .indigo),
    ]
    static let genres = ["Pop", "Rap FR", "Jazz", "Classique", "Électro", "Rock"]
}

private struct AppleMusicGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(AppleMusicTokens.amSystemGreen.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}

