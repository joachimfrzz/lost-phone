import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredThreads`).
struct LpspVendoredThreadsRootView: View {
    var body: some View {
        VendoredThreadsContentView()
    }
}
