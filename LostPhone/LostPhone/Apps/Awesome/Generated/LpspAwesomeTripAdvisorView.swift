import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/travel/tripadvisor/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeTripAdvisorView: View {
    var body: some View {
        TabView {
            TripAdvisorTabHost(title: "Explore", icon: "safari.fill") { TripAdvisorScreenFactory.screen(for: "Explore") }
                .tabItem { Label("Explore", systemImage: "safari.fill") }
            TripAdvisorTabHost(title: "Search", icon: "magnifyingglass") { TripAdvisorScreenFactory.screen(for: "Search") }
                .tabItem { Label("Search", systemImage: "magnifyingglass") }
            TripAdvisorTabHost(title: "Trips", icon: "suitcase.fill") { TripAdvisorScreenFactory.screen(for: "Trips") }
                .tabItem { Label("Trips", systemImage: "suitcase.fill") }
            TripAdvisorTabHost(title: "Review", icon: "square.and.pencil") { TripAdvisorScreenFactory.screen(for: "Review") }
                .tabItem { Label("Review", systemImage: "square.and.pencil") }
            TripAdvisorTabHost(title: "More", icon: "ellipsis") { TripAdvisorScreenFactory.screen(for: "More") }
                .tabItem { Label("More", systemImage: "ellipsis") }
        }
        .tint(TripAdvisorTokens.taTextPrimary)
    }
}

private struct TripAdvisorTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum TripAdvisorScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("explor") || t.contains("accueil") || t.contains("home") || t.contains("flight") || t.contains("airport"):
            TripAdvisorExploreScreen()
        case let t where t.contains("search") || t.contains("recherch"):
            TripAdvisorSearchScreen()
        case let t where t.contains("trip") || t.contains("voyage") || t.contains("book") || t.contains("wish") || t.contains("saved"):
            TripAdvisorTripsScreen()
        default:
            TripAdvisorGenericListScreen(title: tab)
        }
    }
}
private enum TripAdvisorTokens {
    static let taCanvas = Color.white                                    // #FFFFFF
    static let taSurface = Color(red: 0.949, green: 0.949, blue: 0.949)   // #F2F2F2
    static let taDivider = Color(red: 0.878, green: 0.878, blue: 0.878)   // #E0E0E0
    static let taSurfacePressed = Color(red: 0.910, green: 0.910, blue: 0.910)  // #E8E8E8
    static let taTextPrimary = Color.black                                    // #000000
    static let taTextSecondary = Color(red: 0.420, green: 0.420, blue: 0.420)   // #6B6B6B
    static let taTextTertiary = Color(red: 0.608, green: 0.608, blue: 0.608)   // #9B9B9B
    static let taGreen = Color(red: 0.204, green: 0.878, blue: 0.631)   // #34E0A1
    static let taGreenPressed = Color(red: 0.129, green: 0.773, blue: 0.537)   // #21C589
    static let taOwlBlack = Color.black                                    // #000000
    static let taEmptyBubble = Color(red: 0.851, green: 0.851, blue: 0.851)   // #D9D9D9
    static let taErrorRed = Color(red: 0.839, green: 0.071, blue: 0.180)   // #D6122E
}

private enum TripAdvisorFonts {
    static let taTitleLarge = Font.system(size: 28, weight: .regular)
    static let taPlaceHero = Font.system(size: 24, weight: .regular)
    static let taSection = Font.system(size: 22, weight: .regular)
    static let taPlaceName = Font.system(size: 17, weight: .regular)
    static let taCardTitle = Font.system(size: 16, weight: .regular)
    static let taReviewBody = Font.system(size: 15, weight: .regular)
    static let taButton = Font.system(size: 16, weight: .regular)
    static let taButtonSec = Font.system(size: 15, weight: .regular)
    static let taMeta = Font.system(size: 13, weight: .regular)
    static let taTab = Font.system(size: 11, weight: .regular)
    static let taBadge = Font.system(size: 11, weight: .regular)
    static let taCaption = Font.system(size: 12, weight: .regular)
}
private struct TripAdvisorExploreScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(alignment: .leading, spacing: 16) {
                    TripAdvisorSearchBar(accent: TripAdvisorTokens.taTextPrimary)
                    Text("Logements populaires").font(.title2.bold()).padding(.horizontal)
                    LazyVStack(spacing: 20) { ForEach(TripAdvisorDemoData.listings) { item in TripAdvisorListingCard(item: item) } }.padding(.horizontal)
                }.padding(.vertical)
            }.navigationTitle("TripAdvisor")
        }
    }
}

private struct TripAdvisorSearchBar: View {
    let accent: Color
    var body: some View {
        HStack {
            Image(systemName: "magnifyingglass").foregroundStyle(accent)
            VStack(alignment: .leading) { Text("Où ?").font(.headline); Text("Dates · Voyageurs").font(.caption).foregroundStyle(.secondary) }
            Spacer(); Image(systemName: "slider.horizontal.3")
        }.padding().background(RoundedRectangle(cornerRadius: 28).strokeBorder(Color.secondary.opacity(0.3))).padding(.horizontal)
    }
}

private struct TripAdvisorListingCard: View {
    let item: TripAdvisorListing
    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            RoundedRectangle(cornerRadius: 12).fill(item.color.opacity(0.35)).frame(height: 200)
            HStack { Text(item.location).font(.subheadline); Spacer(); HStack(spacing: 2) { Image(systemName: "star.fill"); Text(item.rating) }.font(.subheadline) }
            Text(item.title).font(.headline)
            Text("\(item.price) par nuit").font(.subheadline).foregroundStyle(.secondary)
        }
    }
}

private struct TripAdvisorSearchScreen: View {
    var body: some View { NavigationStack { TripAdvisorSearchBar(accent: TripAdvisorTokens.taTextPrimary).padding().navigationTitle("Rechercher") } }
}

private struct TripAdvisorTripsScreen: View {
    var body: some View {
        NavigationStack { ContentUnavailableView("Aucun voyage", systemImage: "suitcase", description: Text("Vos réservations apparaîtront ici")).navigationTitle("Voyages") }
    }
}

private struct TripAdvisorListing: Identifiable { let id = UUID(); let title: String; let location: String; let price: String; let rating: String; let color: Color }
private enum TripAdvisorDemoData {
    static let listings: [TripAdvisorListing] = [
        .init(title: "Appartement haussmannien", location: "Paris, France", price: "145 €", rating: "4,92", color: .pink),
        .init(title: "Villa avec piscine", location: "Nice, France", price: "220 €", rating: "4,88", color: .cyan),
        .init(title: "Chalet cosy", location: "Chamonix, France", price: "180 €", rating: "4,95", color: .brown),
    ]
}

private struct TripAdvisorGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(TripAdvisorTokens.taTextPrimary.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}
