import SwiftUI

/// Point d'entrée Lost Phone — clone vendored (préfixe `VendoredAppleMusic`).
struct LpspVendoredAppleMusicRootView: View {
    var body: some View {
        VendoredAppleMusicContentView()
    }
}
