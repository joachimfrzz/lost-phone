import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/travel/expedia/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeExpediaView: View {
    var body: some View {
        TabView {
            ExpediaTabHost(title: "Search", icon: "magnifyingglass") { ExpediaScreenFactory.screen(for: "Search") }
                .tabItem { Label("Search", systemImage: "magnifyingglass") }
            ExpediaTabHost(title: "Saved", icon: "heart") { ExpediaScreenFactory.screen(for: "Saved") }
                .tabItem { Label("Saved", systemImage: "heart") }
            ExpediaTabHost(title: "Trips", icon: "suitcase") { ExpediaScreenFactory.screen(for: "Trips") }
                .tabItem { Label("Trips", systemImage: "suitcase") }
            ExpediaTabHost(title: "Support", icon: "questionmark.circle") { ExpediaScreenFactory.screen(for: "Support") }
                .tabItem { Label("Support", systemImage: "questionmark.circle") }
            ExpediaTabHost(title: "Account", icon: "person.crop.circle") { ExpediaScreenFactory.screen(for: "Account") }
                .tabItem { Label("Account", systemImage: "person.crop.circle") }
        }
        .tint(ExpediaTokens.expTextPrimary)
    }
}

private struct ExpediaTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum ExpediaScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("explor") || t.contains("accueil") || t.contains("home") || t.contains("flight") || t.contains("airport"):
            ExpediaExploreScreen()
        case let t where t.contains("search") || t.contains("recherch"):
            ExpediaSearchScreen()
        case let t where t.contains("trip") || t.contains("voyage") || t.contains("book") || t.contains("wish") || t.contains("saved"):
            ExpediaTripsScreen()
        default:
            ExpediaGenericListScreen(title: tab)
        }
    }
}
private enum ExpediaTokens {
    static let expCanvas = white                                    // #FFFFFF
    static let expSurfaceGray = Color(red: 0.961, green: 0.969, blue: 0.980)  // #F5F7FA
    static let expSurfacePressed = Color(red: 0.925, green: 0.937, blue: 0.957) // #ECEFF4
    static let expDivider = Color(red: 0.890, green: 0.906, blue: 0.929)  // #E3E7ED
    static let expDarkCanvas = Color(red: 0.055, green: 0.067, blue: 0.086)  // #0E1116
    static let expDarkSurface1 = Color(red: 0.086, green: 0.106, blue: 0.133)  // #161B22
    static let expDarkSurface2 = Color(red: 0.122, green: 0.149, blue: 0.188)  // #1F2630
    static let expDarkDivider = Color(red: 0.165, green: 0.196, blue: 0.243)  // #2A323E
    static let expTextPrimary = Color(red: 0.102, green: 0.122, blue: 0.149)  // #1A1F26
    static let expTextSecondary = Color(red: 0.353, green: 0.396, blue: 0.451)  // #5A6573
    static let expTextTertiary = Color(red: 0.541, green: 0.584, blue: 0.639)  // #8A95A3
    static let expDarkTextPrimary = Color(red: 0.910, green: 0.922, blue: 0.937) // #E8EBEF
    static let expDarkTextSecondary = Color(red: 0.604, green: 0.643, blue: 0.698) // #9AA4B2
    static let expYellow = Color(red: 1.000, green: 0.788, blue: 0.302)  // #FFC94D
    static let expYellowDeep = Color(red: 1.000, green: 0.702, blue: 0.102)  // #FFB31A
    static let expActionBlue = Color(red: 0.086, green: 0.408, blue: 0.890)  // #1668E3
    static let expActionPressed = Color(red: 0.059, green: 0.310, blue: 0.690)  // #0F4FB0
    static let expNavy = Color(red: 0.000, green: 0.208, blue: 0.373)  // #00355F
    static let expNavySoft = Color(red: 0.078, green: 0.255, blue: 0.420)  // #14416B
    static let expOneKeyGold = Color(red: 0.961, green: 0.773, blue: 0.094)  // #F5C518
    static let expSuccess = Color(red: 0.102, green: 0.545, blue: 0.294)  // #1A8B4B
    static let expError = Color(red: 0.851, green: 0.227, blue: 0.227)  // #D93A3A
    static let expWarning = Color(red: 0.910, green: 0.514, blue: 0.047)  // #E8830C
}

private enum ExpediaFonts {
    static let expDisplay = Font.system(size: 32, weight: .heavy)
    static let expScreenTitle = Font.system(size: 26, weight: .heavy)
    static let expSection = Font.system(size: 22, weight: .bold)
    static let expCardTitle = Font.system(size: 18, weight: .bold)
    static let expBody = Font.system(size: 16, weight: .regular)
    static let expCardSubtitle = Font.system(size: 15, weight: .semibold)
    static let expMeta = Font.system(size: 14, weight: .regular)
    static let expStrike = Font.system(size: 13, weight: .regular)
    static let expBadge = Font.system(size: 12, weight: .semibold)
    static let expOneKeyLine = Font.system(size: 11, weight: .bold)
    static let expButton = Font.system(size: 16, weight: .bold)
    static let expTab = Font.system(size: 10, weight: .semibold)
    static let expPriceNow = Font.system(size: 18, weight: .heavy)
    static let expScoreNum = Font.system(size: 15, weight: .heavy)
}
private struct ExpediaExploreScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(alignment: .leading, spacing: 16) {
                    ExpediaSearchBar(accent: ExpediaTokens.expTextPrimary)
                    Text("Logements populaires").font(.title2.bold()).padding(.horizontal)
                    LazyVStack(spacing: 20) { ForEach(ExpediaDemoData.listings) { item in ExpediaListingCard(item: item) } }.padding(.horizontal)
                }.padding(.vertical)
            }.navigationTitle("Expedia")
        }
    }
}

private struct ExpediaSearchBar: View {
    let accent: Color
    var body: some View {
        HStack {
            Image(systemName: "magnifyingglass").foregroundStyle(accent)
            VStack(alignment: .leading) { Text("Où ?").font(.headline); Text("Dates · Voyageurs").font(.caption).foregroundStyle(.secondary) }
            Spacer(); Image(systemName: "slider.horizontal.3")
        }.padding().background(RoundedRectangle(cornerRadius: 28).strokeBorder(Color.secondary.opacity(0.3))).padding(.horizontal)
    }
}

private struct ExpediaListingCard: View {
    let item: ExpediaListing
    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            RoundedRectangle(cornerRadius: 12).fill(item.color.opacity(0.35)).frame(height: 200)
            HStack { Text(item.location).font(.subheadline); Spacer(); HStack(spacing: 2) { Image(systemName: "star.fill"); Text(item.rating) }.font(.subheadline) }
            Text(item.title).font(.headline)
            Text("\(item.price) par nuit").font(.subheadline).foregroundStyle(.secondary)
        }
    }
}

private struct ExpediaSearchScreen: View {
    var body: some View { NavigationStack { ExpediaSearchBar(accent: ExpediaTokens.expTextPrimary).padding().navigationTitle("Rechercher") } }
}

private struct ExpediaTripsScreen: View {
    var body: some View {
        NavigationStack { ContentUnavailableView("Aucun voyage", systemImage: "suitcase", description: Text("Vos réservations apparaîtront ici")).navigationTitle("Voyages") }
    }
}

private struct ExpediaListing: Identifiable { let id = UUID(); let title: String; let location: String; let price: String; let rating: String; let color: Color }
private enum ExpediaDemoData {
    static let listings: [ExpediaListing] = [
        .init(title: "Appartement haussmannien", location: "Paris, France", price: "145 €", rating: "4,92", color: .pink),
        .init(title: "Villa avec piscine", location: "Nice, France", price: "220 €", rating: "4,88", color: .cyan),
        .init(title: "Chalet cosy", location: "Chamonix, France", price: "180 €", rating: "4,95", color: .brown),
    ]
}

private struct ExpediaGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(ExpediaTokens.expTextPrimary.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}
