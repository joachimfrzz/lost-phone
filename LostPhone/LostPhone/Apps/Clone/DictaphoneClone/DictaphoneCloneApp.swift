import SwiftUI

// Entrée Lost Phone — UI vendored mushthak/voice-memos-clone-swiftui
struct DictaphoneCloneAppView: View {
    var body: some View {
        DictaphoneRootView()
    }
}

struct LpspDictaphoneView: View {
    var body: some View {
        DictaphoneCloneAppView()
    }
}
