import SwiftUI

// Quasi-conformant UI — Meliwat/awesome-ios-design-md/travel/flighty/DESIGN-swiftui.md
// Generated by generate_awesome_apps_v2.py — do not edit by hand
struct LpspAwesomeFlightyView: View {
    var body: some View {
        TabView {
            FlightyTabHost(title: "Flights", icon: "airplane") { FlightyScreenFactory.screen(for: "Flights") }
                .tabItem { Label("Flights", systemImage: "airplane") }
            FlightyTabHost(title: "Search", icon: "magnifyingglass") { FlightyScreenFactory.screen(for: "Search") }
                .tabItem { Label("Search", systemImage: "magnifyingglass") }
            FlightyTabHost(title: "Airport", icon: "building.2.fill") { FlightyScreenFactory.screen(for: "Airport") }
                .tabItem { Label("Airport", systemImage: "building.2.fill") }
            FlightyTabHost(title: "Profile", icon: "person.fill") { FlightyScreenFactory.screen(for: "Profile") }
                .tabItem { Label("Profile", systemImage: "person.fill") }
        }
        .tint(FlightyTokens.fltTextPrimary)
    }
}

private struct FlightyTabHost<Content: View>: View {
    let title: String
    let icon: String
    @ViewBuilder var content: () -> Content
    var body: some View { content() }
}

private enum FlightyScreenFactory {
    @ViewBuilder
    static func screen(for tab: String) -> some View {
        switch tab.lowercased() {
        case let t where t.contains("explor") || t.contains("accueil") || t.contains("home") || t.contains("flight") || t.contains("airport"):
            FlightyExploreScreen()
        case let t where t.contains("search") || t.contains("recherch"):
            FlightySearchScreen()
        case let t where t.contains("trip") || t.contains("voyage") || t.contains("book") || t.contains("wish") || t.contains("saved"):
            FlightyTripsScreen()
        default:
            FlightyGenericListScreen(title: tab)
        }
    }
}
private enum FlightyTokens {
    static let fltCanvas = Color(red: 0.043, green: 0.043, blue: 0.059)  // #0B0B0F
    static let fltSurface1 = Color(red: 0.102, green: 0.102, blue: 0.122)  // #1A1A1F
    static let fltSurface2 = Color(red: 0.133, green: 0.133, blue: 0.157)  // #222228
    static let fltSurface3 = Color(red: 0.173, green: 0.173, blue: 0.204)  // #2C2C34
    static let fltDivider = Color(red: 0.180, green: 0.180, blue: 0.212)  // #2E2E36
    static let fltTextPrimary = white                              // #FFFFFF
    static let fltTextSecondary = Color(red: 0.557, green: 0.557, blue: 0.588) // #8E8E96
    static let fltTextTertiary = Color(red: 0.353, green: 0.353, blue: 0.384) // #5A5A62
    static let fltBlue = Color(red: 0.039, green: 0.518, blue: 1.000) // #0A84FF
    static let fltBluePressed = Color(red: 0.000, green: 0.400, blue: 0.800) // #0066CC
    static let fltOnTime = Color(red: 0.188, green: 0.820, blue: 0.345)  // #30D158
    static let fltDelay = Color(red: 1.000, green: 0.839, blue: 0.039)  // #FFD60A
    static let fltCancelled = Color(red: 1.000, green: 0.271, blue: 0.227)  // #FF453A
    static let fltMapLand = Color(red: 0.086, green: 0.086, blue: 0.106) // #16161B
    static let fltMapGraticule = Color(red: 0.122, green: 0.122, blue: 0.149) // #1F1F26
    static let fltBlueGlow = fltBlue.opacity(0.45)
}

private enum FlightyFonts {
    static let fltTimeHero = Font.system(size: 32, weight: .bold)
    static let fltTitleLarge = Font.system(size: 28, weight: .bold)
    static let fltRouteCode = Font.system(size: 24, weight: .bold)
    static let fltSection = Font.system(size: 22, weight: .bold)
    static let fltCardTitle = Font.system(size: 17, weight: .semibold)
    static let fltBody = Font.system(size: 15, weight: .regular)
    static let fltButton = Font.system(size: 16, weight: .semibold)
    static let fltStatus = Font.system(size: 13, weight: .bold)
    static let fltGate = Font.system(size: 15, weight: .semibold)
    static let fltMeta = Font.system(size: 13, weight: .regular)
    static let fltOnTimePct = Font.system(size: 20, weight: .bold)
    static let fltTab = Font.system(size: 11, weight: .semibold)
    static let fltTimestamp = Font.system(size: 11, weight: .semibold)
}
private struct FlightyExploreScreen: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(alignment: .leading, spacing: 16) {
                    FlightySearchBar(accent: FlightyTokens.fltTextPrimary)
                    Text("Logements populaires").font(.title2.bold()).padding(.horizontal)
                    LazyVStack(spacing: 20) { ForEach(FlightyDemoData.listings) { item in FlightyListingCard(item: item) } }.padding(.horizontal)
                }.padding(.vertical)
            }.navigationTitle("Flighty")
        }
    }
}

private struct FlightySearchBar: View {
    let accent: Color
    var body: some View {
        HStack {
            Image(systemName: "magnifyingglass").foregroundStyle(accent)
            VStack(alignment: .leading) { Text("Où ?").font(.headline); Text("Dates · Voyageurs").font(.caption).foregroundStyle(.secondary) }
            Spacer(); Image(systemName: "slider.horizontal.3")
        }.padding().background(RoundedRectangle(cornerRadius: 28).strokeBorder(Color.secondary.opacity(0.3))).padding(.horizontal)
    }
}

private struct FlightyListingCard: View {
    let item: FlightyListing
    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            RoundedRectangle(cornerRadius: 12).fill(item.color.opacity(0.35)).frame(height: 200)
            HStack { Text(item.location).font(.subheadline); Spacer(); HStack(spacing: 2) { Image(systemName: "star.fill"); Text(item.rating) }.font(.subheadline) }
            Text(item.title).font(.headline)
            Text("\(item.price) par nuit").font(.subheadline).foregroundStyle(.secondary)
        }
    }
}

private struct FlightySearchScreen: View {
    var body: some View { NavigationStack { FlightySearchBar(accent: FlightyTokens.fltTextPrimary).padding().navigationTitle("Rechercher") } }
}

private struct FlightyTripsScreen: View {
    var body: some View {
        NavigationStack { ContentUnavailableView("Aucun voyage", systemImage: "suitcase", description: Text("Vos réservations apparaîtront ici")).navigationTitle("Voyages") }
    }
}

private struct FlightyListing: Identifiable { let id = UUID(); let title: String; let location: String; let price: String; let rating: String; let color: Color }
private enum FlightyDemoData {
    static let listings: [FlightyListing] = [
        .init(title: "Appartement haussmannien", location: "Paris, France", price: "145 €", rating: "4,92", color: .pink),
        .init(title: "Villa avec piscine", location: "Nice, France", price: "220 €", rating: "4,88", color: .cyan),
        .init(title: "Chalet cosy", location: "Chamonix, France", price: "180 €", rating: "4,95", color: .brown),
    ]
}

private struct FlightyGenericListScreen: View {
    let title: String
    var body: some View {
        NavigationStack {
            List(0..<8, id: \.self) { i in
                HStack {
                    RoundedRectangle(cornerRadius: 8).fill(FlightyTokens.fltTextPrimary.opacity(0.15)).frame(width: 44, height: 44)
                    VStack(alignment: .leading) {
                        Text("\(title) \(i + 1)").font(.system(size: 17, weight: .semibold))
                        Text("Mis à jour récemment").font(.system(size: 14)).foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle(title)
        }
    }
}
